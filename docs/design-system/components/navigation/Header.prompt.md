Site-wide header: logo mark, 5-way nav, language switcher, contact CTA. `transparent` overlays it on a hero image/video; otherwise it sits on solid charcoal.

```jsx
<Header active="packages" locale="EN" onLocaleChange={setLocale} />
```
