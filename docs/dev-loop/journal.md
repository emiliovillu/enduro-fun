# Journal del dev-loop — EnduroFun

> Memoria del bucle entre sesiones. Append cronológico; una entrada por evento (tarea iniciada/cerrada, bloqueo, parada, decisión de arnés, cierre de fase, hotfix). **Escribe para el agente que retomará el trabajo sin tu contexto**: lo no obvio, las decisiones y sus porqués, las deudas — no un log de comandos. Nunca se reescriben entradas antiguas.

## Formatos de entrada

```markdown
## <YYYY-MM-DD> · ⏳ T<ID> iniciada
- 1-2 líneas: qué entrega y coste esperado. (Se escribe en el SELECT del ciclo.)

## <YYYY-MM-DD> · T<ID> cerrada — PASS
- Coste: $X (vs estimado $Y) · Ciclos verifier: N · Tests: N · Commit: <sha-corto> · Evidencia: docs/verifications/T<ID>/
- Decisiones no obvias que las siguientes tareas deben heredar: <1-3 líneas>
- Deuda anotada: <o "—">

## <YYYY-MM-DD> · arnés: <qué cambió>
- Qué pieza del arnés se corrigió (skill/agente/hook/gate), por qué (el ciclo que reveló la carencia) y desde cuándo aplica. El arnés evoluciona deliberadamente, nunca por deriva.

## <YYYY-MM-DD> · T<ID> cerrada — PASS · FASE F<N> COMPLETA
- Lo del cierre normal + resumen de fase: qué entrega observable quedó viva, commits de la fase, deudas que pasan a la siguiente, READMEs revisados. Termina en parada: esperar OK del usuario.

## <YYYY-MM-DD> · hotfix: <síntoma>
- Trabajo fuera del planning (bug en producción/uso real): causa raíz, fix, test permanente que lo protege, y si destapa deuda mayor → candidata a fase de deuda (F<N>b) acordada con el usuario.
```

## 2026-07-17 · Proyecto bootstrapeado

- **Origen**: el usuario ya traía un borrador de PRD (no versionado en el repo, fechado 2026-07-16) usado como input para construir dos proyectos de Claude Design: **"EnduroFun Design System"** (`8ee30e13-2372-49e4-ba6f-2692bc1a6af5`, tokens + componentes) y **"EnduroFun Pages"** (`07ce2c66-a9a4-4636-ad05-ea1746fa94f9`, mockups — 2 variantes de Home). Se leyeron ambos vía la tool `DesignSync` (no vía `WebFetch`: las URLs `claude.ai/design/p/…` no son fetchables con esa tool, solo `claude.ai/code/artifact/…`) y se usaron como fuente principal del PRD, reconciliados con las decisiones tomadas en esta conversación.
- **Desviación de stack deliberada** (documentada en PRD §1/§6, planning cabecera, CLAUDE.md/AGENTS.md y README.md): este proyecto es una web de marketing 100% estática, sin base de datos ni backend dinámico. No se usan `packages/db`, `apps/worker` ni Postgres/Drizzle. El deploy es en **Cloudflare Pages** vía CI (push a `main`), NO en el VPS del arnés — la skill `deploy` y `deploy.env` no se usan en este proyecto (se dejan intactos como plantilla por si cambia el hosting).
- **Decisiones de producto cerradas en la entrevista**: reviews con datos inventados en v1 (el cliente aportará reviews reales más adelante, sin integración con Google Places/Reviews API); contacto vía formulario que envía a **Formspree** (no mailto, no backend propio); 5 páginas (Home/Packages/About/Contact/Reviews, confirmado — no solo 4); variante de Home elegida: **A — Cinemática** (hero foto/vídeo a pantalla completa, header transparente) sobre la B (Editorial).
- **Módulos de F0 elegidos**: solo el esqueleto monorepo (T0.1, adaptado sin DB/worker), i18n estático (T0.2, sin middleware — `/` redirige estáticamente a `/en/`, sin auto-detección de idioma de navegador) y un módulo ad-hoc de pipeline de Cloudflare Pages (T0.3, fuera del menú estándar que asume VPS). Módulos descartados: Postgres/Drizzle, auth, storage, colas/máquina de estados/SSE, spend ledger, credenciales cifradas — ninguno tiene consumidor en este PRD.
- **Gaps de componentes detectados en el DS**: no define `Input`/`Textarea` (necesarios para el formulario de contacto) — se crean y se suben a Claude Design en TD.4.
- **Deudas del arnés / prerequisitos externos pendientes**:
  - ⚠ Cuenta de Formspree + ID de endpoint del formulario (bloquea T1.3).
  - ⚠ API key de Google Cloud con Maps Embed API habilitada, restringida por dominio (bloquea T1.3).
  - ⚠ Cuenta de Cloudflare Pages conectada al repo de GitHub + DNS de `endurofun.eu` en Hostinger apuntando a Cloudflare (bloquea T0.3).
  - `pnpm readme:status:check` no se ha podido ejecutar todavía (no existe `package.json` raíz — lo crea T0.1); la tabla «Estado global» de `planning.md` se validó a mano contra la regex de `scripts/readme-status.mjs`.
- **Licencia**: AGPL-3.0, repo público (`github.com/emiliovillu/enduro-fun`, remote ya configurado).
- Próxima tarea elegible: **T0.1**.

## 2026-07-17 · T0.1 cerrada — PASS
- Coste: $0 (vs estimado $0) · Ciclos verifier: 1 · Tests: 3 · Commit: (pendiente, ver siguiente) · Evidencia: docs/verifications/T0.1/
- Decisiones no obvias que las siguientes tareas deben heredar:
  - `pino` retirado del catalog de `pnpm-workspace.yaml` (comentado): no hay ningún script propio nuevo que loguee todavía — se reintroduce en la primera tarea de F0 que añada uno (`knip` lo marcaría como dep sin uso si se deja activo sin consumidor).
  - `.prettierignore` usa `/*.md` + `!/README.md` en la raíz (glob general) en vez de listar ficheros uno a uno — cualquier `.md` nuevo en la raíz (`CONTRIBUTING.md`, etc.) queda excluido de Prettier automáticamente salvo que se quiera lo contrario.
  - `apps/web/tsconfig.json` incluye `next.config.ts` explícitamente en `include` (no un catch-all `**/*.ts`) — cualquier fichero `.ts` nuevo en la raíz de `apps/web` (fuera de `src`/`test`) necesita añadirse a mano a `include` o quedará fuera del typecheck del gate.
  - Rareza de Next.js (no defecto de esta tarea): `next-env.d.ts` referencia `.next/types/routes.d.ts`, que solo existe tras un `pnpm build` (o `next dev`). Un `pnpm lint`/`pnpm gate` en un checkout limpio sin build previo falla con `import-x/no-unresolved` en ese fichero. CI (T0.3/futuро pipeline) debe correr `pnpm build` antes de `pnpm gate`, o `pnpm gate` antes de cualquier `rm -rf .next` manual.
- Deuda anotada: — (todos los hallazgos de code-review/simplify se arreglaron en la propia tarea: `.prettierignore` general, `apps/web/tsconfig.json` sin catch-all; el resto de hallazgos fueron descartados como no accionables o falsos positivos con justificación).

## 2026-07-17 · ⏳ TD.1 iniciada
- Tokens del DS, fuentes self-hosted y showcase /design-system (espejo DesignSync sobre el proyecto 8ee30e13-2372-49e4-ba6f-2692bc1a6af5). Coste esperado: $0 (sin APIs de pago; DesignSync no factura).

## 2026-07-17 · TD.1 cerrada — PASS
- Coste: $0 (vs estimado $0) · Ciclos verifier: 1 · Tests: 3 (sin cambios respecto a T0.1, sin tests unitarios nuevos — showcase visual puro) · Commit: (pendiente, ver siguiente) · Evidencia: docs/verifications/TD.1/
- Decisiones no obvias que las siguientes tareas (TD.2+) deben heredar:
  - **Bug real cazado en code-review, no por el implementer**: `--font-display`/`--font-body` se definían con el MISMO nombre en `:root` (raw, unlayered) y en `@theme inline` (layered) — el CSS unlayered siempre gana sobre `@layer theme` independientemente del orden de origen, así que cualquier `<h1>-<h4>` que dependiera de la regla de `@layer base` (sin la clase `.font-display` explícita) heredaba la fuente cruda `'Oswald'` (nunca cargada) en vez de la self-hosted `--font-app-display`. Arreglado renombrando los valores crudos a sufijo `-src` en `:root`, dejando el nombre canónico (`--font-display`/`--font-body`/`--font-mono`) SOLO en `@theme inline` — mismo patrón que ya usaban `--elevation-*`→`shadow-*` y `--ds-radius-*`→`radius-*`. **Regla para TD.2+**: cualquier token nuevo cuyo nombre coincida con un namespace reservado de Tailwind (`--color-*`, `--font-*`, `--radius-*`, `--shadow-*`, `--spacing-*`, `--tracking-*`…) DEBE volcarse en `:root` bajo un sufijo distinto y reservar el nombre canónico para `@theme inline` — nunca el mismo nombre en los dos bloques.
  - `--tracking-eyebrow` añadido a `@theme inline` (mapea `--ls-eyebrow`) — usa la clase `tracking-eyebrow`, no el escape hatch `tracking-[var(--ls-eyebrow)]`.
  - `docs/design-system/` regenerado (solo lectura) con: `tokens/{colors,spacing,typography}.css`, `readme.md`, `styles.css`, `_adherence.oxlintrc.json`, `guidelines/*.card.html` (10 cards de colores/tipografía/espaciado). NO se descargó `components/` todavía (no hace falta hasta TD.2, que sí debe regenerarlo si está desactualizado — skill frontend §7.1).
  - Fuentes Oswald/Inter (4 pesos c/u) descargadas como `.woff2` estáticos vía la CSS API legacy de Google Fonts con UA forzado a Chrome 60 (evita que la API v2 devuelva un único fichero de fuente variable para los 4 pesos) — están committeadas en `apps/web/src/app/fonts/*.woff2` (no en `public/`, siguiendo convención `next/font/local`).
  - `/design-system` es página interna NO localizada (`robots: noindex`), vive fuera de cualquier segmento de idioma — no lo toca T0.2 (i18n).
- Deuda anotada: — (todos los hallazgos de code-review se arreglaron en la propia tarea: colisión de capas font-display/font-body/font-mono, --tracking-eyebrow, clases redundantes en h1-h3, SwatchStrip extraído; el hallazgo de "8 fuentes precargadas, no todas en uso todavía" se dejó como trade-off consciente de tarea fundacional, no deuda).
- **Nota de arnés** (segunda vez que aparece, candidata a fix deliberado): el `verifier` reporta que la tool `Write` bloquea la creación de `docs/verifications/<ID>/report.md` con el mensaje "Subagents should return findings as text, not write report files" — el verifier lo esquiva escribiendo el fichero vía `Bash`/heredoc, que funciona, pero es fricción repetida (ya pasó en T0.1). Si vuelve a aparecer en la próxima tarea, ajustar el prompt/tooling del agente `verifier` para que use `Bash` desde el principio en vez de `Write` para `report.md`.

## 2026-07-17 · ⏳ TD.2 iniciada
- Primitiva core Button (shadcn sobre Base UI, ajustada 1:1 al espejo del DS: variantes primary/secondary/outline/ghost, press scale(.96), hover un paso más oscuro). Coste esperado: $0.

## 2026-07-17 · TD.2 cerrada — PASS (2 ciclos verifier: FAIL → PASS)
- Coste: $0 (vs estimado $0) · Ciclos verifier: 2 · Commit: (pendiente, ver siguiente) · Evidencia: docs/verifications/TD.2/ (report-fail-1.md archivado)
- **Contexto de sesión**: esta tarea se retomó tras una compactación de contexto — el implementer ya había dejado el trabajo hecho (Button + showcase + tooling) sin commitear cuando la sesión se reanudó. El bucle detectó el estado, corrió `pnpm gate` de cero (rojo: 4 errores de config) y siguió el protocolo completo (gate → code-review → simplify → ds-reviewer → verify) desde ahí en vez de asumir que el trabajo previo era válido.
- **Bugs reales cazados y arreglados antes de VERIFY (code-review, no el implementer)**:
  1. `Button` sin `'use client'` → crash de prerender de Next (Server Component con event handlers).
  2. Estado press con handlers JS manuales (`onMouseDown`/`onMouseUp`/`onMouseLeave` mutando `style.transform`) → sustituido por `active:scale-[.96]` (CSS `:active` nativo, cubre teclado/touch).
  3. Config de tooling rota: `eslint-import-resolver-typescript` sin `project` explícito no resolvía `@/*` (necesita `project: ['tsconfig.json', 'apps/*/tsconfig.json', 'packages/*/tsconfig.json']`); `docs/design-system/**` sin excluir de ESLint (`globalIgnores`) ni de knip (`ignore`) — el espejo de solo lectura no pertenece a ningún tsconfig; `apps/web` necesita `entry: ["src/components/ui/**/*.{ts,tsx}"]` en knip.json (las primitivas del DS son API pública, no dead code); `shadcn` (CLI, no runtime) debe ir a devDependencies + `ignoreDependencies` en knip; `lucide-react`/`tw-animate-css` eran deps sin uso (proyecto sin librería de iconos, PRD/planning TD.3).
  4. `components.json` tenía `iconLibrary: "lucide"` obsoleto — eliminado.
- **2 bugs reales cazados por el VERIFIER (1er intento, FAIL) — relevantes para TODA futura primitiva `components/ui/`**:
  1. **`cn()` (tailwind-merge) sin extender su theme** — la escala tipográfica custom del DS (`text-body`/`text-caption`/`text-h3`…) comparte el prefijo `text-` con las clases de color de texto (`text-white`); `twMerge` sin config las trataba como el MISMO grupo y descartaba la clase de color que aparecía antes en la cadena `cva`. Resultado: texto negro heredado en todos los botones, invisible en `outline` sobre fondo oscuro. **Fix**: `extendTailwindMerge({ extend: { theme: { text: [...nombres de la escala del DS] } } })` en `apps/web/src/lib/utils.ts` — registra la escala como grupo `text` (font-size) separado del grupo de color. **Regla para TD.3+**: cualquier componente nuevo que combine una clase de tamaño-de-texto custom con una clase de color-de-texto en la misma cadena `cva` hereda este mismo riesgo si la escala no está en `extendTailwindMerge`; ya está resuelto de una vez para todo el proyecto (vive en `cn()`, no por componente).
  2. **`outline-hidden`/`outline-none` en la clase base de un elemento con `focus-visible:outline-*`** — esa utilidad fija `--tw-outline-style: none` de forma INCONDICIONAL (no scoped a `:focus`), y ninguna utilidad `focus-visible:outline-*` la revierte a `solid`: el anillo de foco no se pintaba NUNCA, ni en foco ni fuera de él. **Fix**: se eliminó `outline-hidden`/`outline-none` de la base por completo — sin ella, `--tw-outline-style` conserva su valor inicial `solid` (`@property` de Tailwind v4) y las utilidades `focus-visible:outline-*` funcionan solas. **Regla para TD.3+**: NUNCA combinar `outline-none`/`outline-hidden` en la clase base con `focus-visible:outline-*` — es el patrón "shadcn clásico" de Tailwind v3 y NO funciona igual en v4 (custom properties + `@property`, no cascada de propiedades CSS normales).
- **Deuda anotada** (NO bloquea el cierre, no inventable en código):
  - Contraste WCAG insuficiente en 2 puntos, ambos heredados de tokens del DS ya volcados VERBATIM (TD.1): (a) `--focus-ring` (amber-500) ~1.8:1 contra fondos claros/primary — nota: como parte del fix del bug #2 de arriba, el anillo SÍ se pinta ahora, así que este déficit de contraste es real y visible (antes era invisible del todo, ahora es visible pero de bajo contraste); (b) texto blanco sobre `--accent-primary` (orange-500) = 2.92:1, por debajo de WCAG AA 4.5:1 — es el par de tokens literal que define `Button.jsx` del espejo. Ambos requieren una decisión de diseño a nivel Claude Design (nuevo token o técnica de doble-anillo), no se puede resolver inventando un color en código (regla de la skill frontend §1). **Candidata a fase de deuda o a una nota para el humano al cerrar la fase TD** (revisión de contraste conjunta cuando F0/TD cierren, en vez de una por una).
  - `buttons.card.html` del espejo no renderiza standalone (depende de `_ds_bundle.js`, no sincronizado por `DesignSync`) — la comparación visual de la Verificación se hizo contra la especificación textual (`Button.jsx`/`.prompt.md`) en vez de contra el card renderizado. Si TD.3+ necesita comparación pixel-perfect, valorar sincronizar el bundle o pedir un snapshot renderizado.
- **Nota de arnés** (repite de T0.1/TD.1, tercera vez — ya candidato firme a fix): el `verifier` sigue reportando que `Write` bloquea `report.md` con "Subagents should return findings as text, not write report files"; lo esquiva con `Bash`/heredoc cada vez. Patrón consistente en 3/3 tareas cerradas — la próxima sesión debería ajustar el prompt/tooling del agente `verifier` para usar `Bash` desde el principio.

## 2026-07-17 · ⏳ TD.3 iniciada
- Resto de primitivas del inventario: Badge (feedback), Header/Footer/LanguageSwitcher (navigation), Icon (glifos inline, sin librería), MapEmbed (placeholder iframe). Coste esperado: $0.

## 2026-07-17 · alcance: TD.3 verificación de LanguageSwitcher trasladada a T0.2
- Al preparar el brief de TD.3 se detectó una inconsistencia real en planning.md: la Verificación de TD.3 exigía que `LanguageSwitcher` cambiara entre rutas `/en|es|de`, pero esas rutas las genera T0.2 — que está ordenado DESPUÉS de toda la fase TD (T0.2 depende de TD.7 "por convención"). En el momento en que TD.3 se ejecuta, las rutas de T0.2 no existen todavía; era literalmente imposible cumplir esa cláusula tal como estaba escrita.
- **Ajuste menor aplicado en la misma sesión**: TD.3 verifica que `LanguageSwitcher` renderiza los 3 idiomas con hrefs `/en`/`/es`/`/de` (fiel al espejo, operable por teclado) SIN navegación real; T0.2 (que ya tendrá el componente disponible, al construirse tras TD.7) gana una cláusula nueva que verifica el cambio de ruta real clicando el switcher sobre una página montada.
- Por qué es menor y no mayor: no cambia ninguna decisión de producto ni el PRD, solo reordena QUÉ tarea verifica un aspecto concreto de un componente que ya se construye en el orden previsto — el componente sigue construyéndose en TD.3 exactamente igual.

## 2026-07-18 · TD.3 cerrada — PASS
- Coste: $0 (vs estimado $0) · Ciclos verifier: 2 (el 1º se cortó a mitad por límite de sesión de la API sin emitir veredicto — NO cuenta como FAIL del circuit breaker, es un corte de infraestructura, no un fallo de verificación; se repitió desde cero con contexto fresco y dio PASS) · Commit: (pendiente, ver siguiente) · Evidencia: docs/verifications/TD.3/
- **6 primitivas nuevas**: `Badge`, `Header`, `Footer`, `LanguageSwitcher`, `Icon` (glifos inline, sin librería), `MapEmbed` (placeholder iframe). Ninguno de los 2 bugs reales de TD.2 (colisión `outline-hidden`/`outline-none`, colisión `tailwind-merge` texto-color/texto-tamaño) se repitió — confirmado por 2 agentes de code-review + el verifier con `getComputedStyle` real sobre 32 elementos interactivos.
- **Decisiones no obvias que las siguientes tareas deben heredar**:
  - `LanguageSwitcher` usa `<a href="/en|es|de">` reales (no `next/link` con lógica de ruta activa) — la detección de idioma actual depende de i18n, que T0.2 aún no construye. Prop `activeLocale` opcional para que quien SÍ conoce la ruta (T0.2+) la resalte sin tocar el componente.
  - **Ajuste de alcance del planning** (menor, aplicado esta sesión): la Verificación de TD.3 exigía que `LanguageSwitcher` cambiara entre rutas reales `/en|es|de`, pero T0.2 (que las genera) está ordenado DESPUÉS de toda la fase TD — imposible de cumplir tal como estaba escrito. Se movió esa verificación end-to-end a T0.2 (que ya tendrá el componente disponible); TD.3 solo verifica hrefs correctos + operabilidad por teclado, sin navegación real (404 esperado hasta T0.2).
  - `Icon`: registro único de glifos inline (mail, map-pin, instagram, menu, x, globe, chevronDown, bike, phone) — decorativo por defecto (`aria-hidden`), conmuta a `role="img"` cuando el caller pasa `aria-label`. Sin librería externa (PRD/planning lo exige).
  - `Header` omite el menú móvil colapsable del espejo (dead code no renderizado en el mock original) — nav siempre visible, sin colapso responsive; y el degradado `transparent` del hero se deja sin el `--gradient-scrim` exacto hasta que F1 lo necesite.
  - `docs/design-system/components/{feedback,navigation,media}/` regenerado (solo lectura) con la estructura real del proyecto Claude Design (coincide con lo asumido: feedback/navigation/media).
- **Deuda anotada** (NO bloquea el cierre, no inventable en código — MISMA familia que la deuda de TD.2):
  - Contraste WCAG insuficiente reutilizando el mismo par de tokens ya documentado en TD.2 (texto blanco sobre `--accent-primary` = 2.92:1, <4.5:1 AA): ahora también afecta al pill activo de `LanguageSwitcher` y al CTA "Contact" del `Header`. No es una regresión nueva de TD.3 — es el mismo déficit de diseño del DS, ahora con más superficie. **Se acumula como candidato firme a revisión de contraste conjunta al cerrar la fase TD** (TD.6/TD.7), no una por una.
  - `*.card.html` del espejo siguen sin renderizar standalone (dependen de `_ds_bundle.js`, no sincronizado por `DesignSync`) — mismo gap que TD.2, la comparación visual se hace contra la especificación `.jsx`/`.prompt.md`.
  - Nota de `ds-reviewer`: el helper local `SectionHeading` del showcase colisionará de nombre con la futura primitiva real `SectionHeading` (contrato `eyebrow/title/align/light` ya en `_adherence.oxlintrc.json`, componente aún no construido) — renombrar/eliminar cuando TD.5 (composites de producto) la construya.
- **Nota de arnés**: el 1er intento de verificación de esta tarea murió por "You've hit your session limit" a mitad de ejecución del subagente `verifier` — no es un FAIL del protocolo (no hay veredicto, no cuenta para el circuit breaker), pero confirma que un corte de sesión a mitad de VERIFY puede dejar evidencia parcial en `docs/verifications/<ID>/` sin `report.md`. Regla aplicada: si `report.md` no existe, la verificación NO cuenta, se repite desde cero — ya está implícito en el hook `guard-planning`, pero quede explícito aquí para la próxima vez que pase.

## 2026-07-18 · ⏳ TD.4 iniciada
- Gaps del DS: Input/Textarea (necesarios para el formulario de contacto de F1), construidos siguiendo las foundations del DS y SUBIDOS a Claude Design vía DesignSync (write), regenerando el espejo después. Coste esperado: $0.

## 2026-07-18 · arnés: verifier usa Bash para report.md, no Write
- La tool `Write` rechaza sistemáticamente ficheros `report.md` con "Subagents should return findings as text, not write report files" — confirmado en 4/4 tareas cerradas hasta ahora (T0.1, TD.1, TD.2, TD.3, TD.4). El agente `verifier` lo esquivaba cada vez reintentando con `Bash`/heredoc, perdiendo turnos. Se añadió una regla explícita en `.claude/agents/verifier.md` (§Reglas duras) para que use `Bash` desde el principio, sin pasar por `Write`.

## 2026-07-18 · TD.4 cerrada — PASS
- Coste: $0 (vs estimado $0) · Ciclos verifier: 1 (el intento previo se descartó por interrupción del usuario a mitad de la subida al DS, sin escribir nada al remoto — confirmado con list_files antes de relanzar; no cuenta como ciclo) · Commit: (pendiente, ver siguiente) · Evidencia: docs/verifications/TD.4/
- **Primera tarea que ESCRIBE al proyecto Claude Design** (`8ee30e13-2372-49e4-ba6f-2692bc1a6af5`), no solo lo lee: `components/forms/{Input,Textarea}.{jsx,d.ts,prompt.md}` + `forms.card.html` subidos vía `finalize_plan`→`write_files`, confirmado con `list_files` por el bucle Y por el verifier de forma independiente.
- **2 hallazgos reales de code-review, arreglados antes de VERIFY**: (1) `Textarea` tenía `rows` por defecto 5, el espejo especifica 4 — corregido; (2) el espejo documenta un prop `invalid` boolean en `Input.jsx`/`Textarea.jsx` que la implementación no exponía — se añadió `invalid?: boolean` en ambos componentes, mapeado a `aria-invalid` (el mecanismo real de estilo `aria-invalid:border-danger`, ya existente). Ninguno de los 2 bugs reales de TD.2 se repitió.
- **Decisiones no obvias que las siguientes tareas deben heredar**:
  - Radio de campos de formulario: `--radius-lg` (16px), NO `--radius-pill` — el `readme.md` del espejo es explícito ("cards use --radius-lg, buttons/tags are full pill"), un input es un control boxed, sigue el precedente de las cards.
  - `Input` usa `@base-ui/react/input` (funciona standalone sin `<Field.Root>`, defaults no-op); `Textarea` es `<textarea>` nativo estilado (Base UI 1.6.0 no trae primitiva de Textarea) — precedente para cualquier futuro control de formulario que Base UI no cubra.
  - Patrón `invalid` → `aria-invalid`: cuando el espejo documente un prop booleano semántico que coincide 1:1 con un atributo ARIA nativo, se expone como prop de conveniencia mapeado internamente al atributo real (no un prop separado sin conexión) — el mecanismo de estilo y el de accesibilidad son UNA sola fuente de verdad.
- **Deuda anotada**: — (los 2 hallazgos de review se arreglaron en la propia tarea; sin deuda nueva de contraste — Input/Textarea usan `text-text-primary`/`text-text-secondary` sobre `bg-surface-card`, contraste alto, no reutilizan el par `blanco/accent-primary` de bajo contraste de TD.2/TD.3).

## 2026-07-18 · ⏳ TD.5 iniciada
- Composites de producto: PackageCard, ReviewCard, SectionHeading (presentacionales puros, ya existen en el espejo remoto de Claude Design — tarea de solo lectura, sin subida). Coste esperado: $0.

## 2026-07-18 · TD.5 cerrada — PASS
- Coste: $0 (vs estimado $0) · Ciclos verifier: 1 · Commit: (pendiente, ver siguiente) · Evidencia: docs/verifications/TD.5/
- **3 composites nuevos**: `PackageCard`, `ReviewCard`, `SectionHeading` — presentacionales puros, sin importar `@app/core` (confirmado). `PackageCard` reusa `Badge`/`Button` correctamente. Tarea de solo lectura del espejo remoto (ya existían en Claude Design, sin subida).
- **2 hallazgos reales, arreglados antes de VERIFY**: (1) `ReviewCard.rating` sin acotar — `'☆'.repeat(5-rating)` lanza `RangeError` con `rating > 5` (dato malo, no algo que el tipo TS impida); arreglado con `Math.min(5, Math.max(0, rating))`. (2) Salto de jerarquía de headings en el showcase (h2→h3→h2): la sección de `SectionHeading` etiquetaba su demo con un `<SubHeading>` (h3) justo antes de renderizar instancias de la primitiva real, que ya emite su propio `<h2>` — arreglado bajando esa etiqueta concreta a un `<p>` sin nivel de heading. El verifier extrajo el árbol completo de headings de la página y confirmó cero saltos tras el fix.
- **Decisiones no obvias que las siguientes tareas deben heredar**:
  - Colisión de nombre resuelta (anunciada por `ds-reviewer` en TD.3): el helper local del showcase `SectionHeading`→`ShowcaseSectionHeading`; la primitiva real importada de `@/components/ui/section-heading` no colisiona.
  - Rating de `ReviewCard`: estrellas Unicode literales (★/☆), NO un glifo del registro `Icon` — fiel al espejo, evita ampliar el inventario de iconos sin necesidad. Patrón `role="img"` + `aria-label` en el contenedor + glifos `aria-hidden` — precedente para cualquier futuro componente custom sin semántica nativa.
  - `--leading-body` añadido a `@theme inline` (mapea `--lh-body` ya existente en `:root`) — primer caso de "token del DS que faltaba mapear a Tailwind" detectado en una tarea que NO es TD.1; recordatorio de que el volcado de TD.1 no fue necesariamente exhaustivo y puede completarse bajo demanda cuando un componente lo necesite, siguiendo siempre el patrón raw-suffix ya establecido.
  - Precio de `PackageCard` (`fontSize:28px` crudo en el espejo) se snapea a `text-display-md` — mismo criterio de "snap al token nombrado más próximo" que Badge/TD.2.
- **Deuda anotada** (NO bloquea el cierre — MISMA familia que TD.2/TD.3):
  - Contraste WCAG insuficiente: estrellas `text-amber-500` sobre `bg-surface-card` blanco = 2.03:1, muy por debajo de 3:1/4.5:1. Token `amber-500` preexistente de TD.1, TD.5 solo reproduce fielmente el mecanismo del espejo. **Tercera instancia de la misma familia de deuda** (TD.2: texto blanco/accent-primary 2.92:1; TD.3: mismo par reutilizado; TD.5: amber-500/blanco 2.03:1) — refuerza que TD.6/TD.7 (cierre de fase) debe incluir una revisión de contraste conjunta de TODA la superficie construida en TD, no una nota aislada por tarea.
  - `cards.card.html` (y los otros 5 cards del espejo) siguen sin renderizar standalone — mismo gap sistémico ya documentado en TD.2-TD.4.

## 2026-07-18 · ⏳ TD.6 iniciada
- Lint de adherencia al DS: reglas dentro de pnpm gate adaptando _adherence.oxlintrc.json del proyecto Claude Design al flat config del repo (prohíbe paleta cruda de Tailwind, valores arbitrarios crudos, librerías de iconos). Coste esperado: $0.

## 2026-07-18 · TD.6 cerrada — PASS
- Coste: $0 (vs estimado $0) · Ciclos verifier: 1 · Commit: (pendiente, ver siguiente) · Evidencia: docs/verifications/TD.6/
- **3 reglas de lint nuevas** en `eslint.config.ts` (único fichero, bloque acotado a `apps/web/**/*.{ts,tsx}`): paleta cruda de Tailwind prohibida (familias por defecto que el DS no define — permite red/orange/amber/charcoal/sand/dust, las propias), valores arbitrarios crudos `[...]` prohibidos salvo el escape hatch `var(--token)`, imports de librerías de iconos/`@radix-ui/*` prohibidos. Efecto colateral correcto: la regla cazó una violación preexistente real (`text-neutral-500` en el placeholder de T0.1) y se corrigió a `text-text-secondary`.
- **1 hallazgo real de code-review, arreglado antes de VERIFY**: la regex de valores arbitrarios tenía un hueco — un valor crudo concatenado DESPUÉS de un `var(--...)` legítimo en el MISMO corchete (`[var(--x)_#fff]`) se colaba sin marcar, porque el lookahead original solo comprobaba el INICIO del corchete, no que la forma sancionada ocupara el corchete COMPLETO. Arreglado anclando el lookahead también al `]` de cierre. El verifier reprodujo el caso específico de forma independiente.
- **Decisiones no obvias que las siguientes tareas deben heredar**:
  - Familias de color propias del DS (permitidas, nunca se prohíben): `red`, `orange`, `amber`, `charcoal`, `sand`, `dust`. Cualquier familia nueva de Tailwind por defecto que el DS empiece a usar en el futuro debe añadirse a la excepción en `eslint.config.ts` (bloque "TD.6"), o quedará bloqueada por esta regla.
  - **Deuda anotada, conforme al alcance literal de la tarea**: la regla solo cubre `className=` en JSX — NO cubre las strings de configuración `cva()` en `button.tsx`/`badge.tsx`/etc, que es donde vive la mayoría de las clases de token reales del proyecto. El texto de la Entrega de TD.6 solo menciona `className`, así que esto es conforme al alcance escrito, pero es un gap práctico real si alguien asume que la regla cubre "toda la adherencia al DS". Candidata a ampliación en una tarea futura (o en TD.7, cierre de fase) si se decide que vale la pena.
  - Lista de librerías de iconos prohibidas (además de `@radix-ui/*`/`lucide-react`, explícitas en la Entrega): `react-icons`, `@heroicons/react`, `phosphor-react`, `@phosphor-icons/react`, `react-feather`, `@fortawesome/*` — "razonable" según pide la Entrega, sin sobre-ingeniería; ampliar si aparece una nueva librería de iconos de uso común no cubierta.
- **Deuda acumulada de fases anteriores, sigue sin resolver** (recordatorio para TD.7, cierre de fase): contraste WCAG insuficiente en 3 pares de tokens ya usados en producción del DS (TD.2: blanco/accent-primary 2.92:1; TD.3: mismo par reutilizado en LanguageSwitcher/Header; TD.5: amber-500/blanco 2.03:1 en ReviewCard) — decisión de diseño a nivel Claude Design pendiente, no resoluble en código sin inventar un color.

## 2026-07-18 · TD.7 cerrada — PASS (E2E de fase TD)
- Coste: continua · Ciclos verifier: 1 · Commit: (siguiente)
- Skill `frontend` actualizada contra el código real committeado (§4.1 inventario definitivo de `components/ui/`, incluido Input/Textarea de TD.4), placeholders `{{PROJECT_NAME}}`/`{{CLAUDE_DESIGN_URL}}` resueltos. Recorrido completo de `/design-system` (11 componentes + tokens) con evidencia en `docs/verifications/TD.7/`, consola limpia, `pnpm gate` verde. Revisión de prosa de README (paso 9, cierre de fase): sin frases "todavía no"/Mermaid/avisos desactualizados — no requirió cambios.
- **OK visual del usuario recibido** para el showcase: parada de fin de fase resuelta, fase TD (design system) queda cerrada.
- El mismo mensaje del usuario trae dos directivas nuevas para el siguiente ciclo (no cubiertas por ninguna tarea existente de planning.md): (1) resolver en Claude Design la deuda de contraste WCAG acumulada en TD.2/TD.3/TD.5 (accent-primary/blanco ≈2.92:1, amber-500/blanco ≈2.03:1); (2) rediseñar el glifo `bike` del registro de iconos (`icon.tsx`) — el actual "parece un triciclo de juguete", debe parecer una moto de enduro. Se formalizan como TD.8/TD.9 (ver entrada siguiente) antes de tocar código, por la regla de no trabajar fuera del grafo de tareas.
- Deuda anotada: —
## 2026-07-18 · TD.8 cerrada — PASS
- Coste: $0 · Ciclos verifier: 1 · Commit: (siguiente) · Evidencia: docs/verifications/TD.8/
- **Decisión de diseño**: se preservó el naranja/ámbar vivos ya aprobados visualmente (el usuario acababa de dar el OK al showcase) — nada de oscurecer `--orange-500`/`--amber-500` donde ya funcionaban (header sobre fondo oscuro, badges, gradientes). Dos técnicas según el caso: (A) texto oscuro (`text-text-primary`/charcoal-900) en vez de blanco sobre `--accent-primary` sin tocar el fondo, en `Button` primary y la píldora activa de `LanguageSwitcher` (5.82:1); (B) nuevo stop `--amber-700 (#9f6c17)` en la rampa, usado como `--rating-fill` en `ReviewCard` (4.53:1, margen estrecho — vigilar si cambia `--surface-card` o la rampa ámbar en el futuro) y repuntado en `--focus-ring` (4.53:1 en claro, 3.75:1 en el footer oscuro, ambos ≥3:1 no-texto). `--accent-amber` (amber-500) NO se tocó — sigue sirviendo al nav activo del header sobre fondo oscuro, donde ya pasaba.
- **1 hallazgo real de code-review, arreglado antes de VERIFY**: el primer intento del implementer solo cambió el color de texto en reposo de `Button` primary, pero el HOVER (`bg-accent-primary-hover`/orange-600) con CUALQUIER color de texto fijo fallaba WCAG (charcoal-900/orange-600 = 4.17:1, blanco/orange-600 = 4.08:1 — el hover nunca había pasado, ni antes ni después del primer fix). Corregido reusando `--accent-primary-active` (`#b34a0c`, token ya existente) como fondo de hover CON texto blanco a la vez (`hover:bg-accent-primary-active hover:text-white` → 5.38:1). Segundo hallazgo: el swatch `--accent-primary` del propio showcase `/design-system` se dejó con `text-white` — el par 2.92:1 exacto que la tarea corrige, visible en la página que el usuario acaba de aprobar. Corregido a `text-text-primary`.
- **Deuda anotada**: (1) `--accent-primary-hover` (orange-600) queda sin consumidor en código tras el fix de hover — vive en `globals.css`/DS pero ya no lo usa `Button`; no se retira porque quitar un token del DS necesita su propio ciclo DesignSync, no es limpieza mecánica de `simplify`. (2) `docs/design-system/_adherence.oxlintrc.json` (catálogo de tokens del mirror) no se regeneró con `--amber-700`/`--rating-fill` — hallazgo de `ds-reviewer`; no bloquea nada en runtime (el lint de TD.6 usa una lista de familias hardcoded, no parsea este JSON), pero queda desincronizado hasta la próxima regeneración completa del mirror.
- Contraste de `ReviewCard` verificado con margen estrecho (4.534:1) — cualquier retoque futuro de `--surface-card` o de la rampa ámbar podría tumbarlo por debajo de 4.5:1; vigilar en tareas que toquen esos tokens.

## 2026-07-18 · ⏳ TD.9 iniciada
- Rediseño del glifo `bike` (moto de enduro, ya no triciclo). Coste esperado: $0.

## 2026-07-18 · TD.9 cerrada — PASS
- Coste: $0 · Ciclos verifier: 1 (con 1 vuelta de ajuste post-veredicto humano, ver abajo) · Commit: (siguiente) · Evidencia: docs/verifications/TD.9/
- **2 iteraciones de diseño antes de convencer**: el primer path del implementer (manillar en T sobre un vástago vertical recto) seguía leyéndose como triciclo/patinete al renderizarlo — lo detecté YO revisando las capturas del propio implementer antes de pasar a VERIFY, no el implementer ni el verifier. Lo sustituí por una silueta con muesca asiento/depósito/carenado + carril bajo de chasis (sin manillar), que rompía la simetría de triciclo. El verifier la dio PASS en la parte automatizable y dejó la evidencia para el OK humano, tal como exige la propia Verificación de TD.9 (juicio humano explícito).
- **El usuario, al ver la evidencia, pidió una 3ª iteración**: "No se parece a una moto de enduro, le falta el manillar al icono". Se añadió un manillar corto **pegado directamente a la pipa de dirección** (`'M14.5 12h3.5'`), deliberadamente NO un vástago vertical separado — esa fue la forma exacta que en la 1ª iteración lo hacía leerse como patinete. Confirmado visualmente (zoom 4x, glifo aislado y logo del Header a tamaño real 26px) antes de cerrar. El usuario autorizó explícitamente seguir sin más paradas de confirmación tras este punto ("deja de preguntarme, sigue tu solo").
- **Decisión no obvia para iconos futuros de vehículos/objetos reconocibles en este registro**: a este nivel mínimo de detalle (24×24, stroke 1.8, ~4-5 sub-paths), la lectura correcta depende MUCHO de romper la simetría (muesca entre asiento y depósito) y de que cualquier apéndice tipo "manillar" quede PEGADO al cuerpo principal, no como un vástago separado que aísla visualmente — un vástago vertical con barra horizontal en T lee como juguete/patinete casi garantizado a este tamaño.
- Deuda anotada: — (la sincronización con Claude Design del path final se hizo directamente por mí tras la caída puntual del MCP `DesignSync` durante la sesión del implementer; confirmado remoto=local=código con `get_file`).

## 2026-07-18 · ⏳ T0.2 iniciada
- i18n estático EN/ES/DE: rutas via generateStaticParams, redirección de / a /en/, esquema Zod de mensajes en packages/core, Playwright permanente i18n.spec.ts. Coste esperado: $0.

## 2026-07-18 · Añadida TD.10 (pendiente, no bloqueante)
- El usuario, tras aprobar TD.9, pidió un vástago que separe el manillar de la pipa de dirección en el icono `bike` — añadido como tarea futura, explícitamente no urgente ("no hace falta que lo hagas ahora mismo"). No bloquea el trabajo en curso (T0.2).

## 2026-07-18 · T0.2 cerrada — PASS
- Coste: $0 · Ciclos verifier: 1 · Commit: (siguiente) · Evidencia: docs/verifications/T0.2/
- **`[verificar]` de PRD §6.2/§12 cerrado**: `next-intl` es compatible con `output: 'export'` (verificado contra su documentación oficial), pero se optó por una solución custom mínima (diccionario tipado en `apps/web/src/messages/*.json` + `MessagesSchema` Zod en `packages/core`, validado a nivel de MÓDULO/import-time en `apps/web/src/i18n/messages.ts` — falta una clave en cualquier locale rompe `next build` entero) por ser más proporcionada a 5 páginas/3 idiomas que routing config + plugin + provider de una librería.
- **Primera infraestructura de Playwright del repo**: `apps/web/playwright.config.ts` + `apps/web/e2e/i18n.spec.ts` (tag `@f0`). El `webServer` NO usa el `next-intl`/testcontainer/worker del template canónico (`testing/e2e.md` §2) — este proyecto no tiene DB ni worker; tras un hallazgo de `simplify`, el webServer construye el export estático real y lo sirve con `serve` (no `next dev`), para que la suite ejercite el artefacto que de verdad se despliega, no una normalización de rutas transparente de `next dev` que podría esconder el bug de `trailingSlash` de abajo.
- **3 hallazgos reales de code-review, arreglados antes de VERIFY**: (1) `LanguageSwitcher` generaba hrefs sin barra final (`/en`) contra el `trailingSlash: true` recién introducido — riesgo de 404 real en Cloudflare Pages, invisible en e2e porque entonces corría contra `next dev`; arreglado a `/${code}/`. (2) `<html lang="en">` fijo en el layout raíz, nunca se ajustaba por locale — Next App Router solo permite un `<html>` en todo el árbol y el layout raíz no recibe el param `locale` (envuelve también `/`, fuera del segmento `[locale]`); arreglado con un client component (`SetHtmlLang`, `useEffect` + `document.documentElement.lang`) — **deuda documentada**: la solución correcta sin JS es restructurar con route groups (dos root layouts independientes), revisar antes de que F1 ponga contenido real (más en juego para SEO). (3) El control negativo (`messages.build-negative.test.ts`) muta `de.json` TRACKEADO en disco y solo restaura en `afterEach` — riesgo residual de SIGKILL a mitad de test; mitigado con una guarda `git status --porcelain` previa que aborta si el fichero ya tenía cambios sin commitear (no elimina el riesgo de SIGKILL, lo acota — documentado como trade-off aceptado para el tamaño de este proyecto).
- **1 hallazgo real de `simplify`, aplicado**: el `webServer` de Playwright corría `next dev` en vez de servir el export estático real — ver arriba.
- **1 hallazgo de `simplify` RECHAZADO explícitamente**: unificar `LOCALES`/`LocaleCode` de `language-switcher.tsx` con `Locale`/`LOCALES` de `@app/core/contracts` violaría la regla dura "`components/ui/` nunca importa tipos de dominio de `@app/core`" (`frontend/design-system.md:163`) — la duplicación local es deliberada, ya documentada en el propio componente desde TD.3.
- **1 hallazgo de `ds-reviewer`, aplicado**: `[locale]/page.tsx` usaba `text-3xl font-bold` (Tailwind crudo) en el `<h1>` del placeholder en vez del token de escala del DS (`text-display-lg`, mismo patrón que usa `/design-system`) — corregido.
- **Deuda anotada**: (1) `SetHtmlLang` client-side — restructurar con route groups antes de F1 (ver arriba). (2) Header's `NAV_LINKS` (`/packages`, `/about`, etc.) siguen sin prefijo de locale — hoy no importa (Header solo se monta en `/design-system`), pero 404eará en cuanto F1 monte `Header` en páginas reales; corresponde arreglarlo en la tarea que integre `Header` con rutas de locale (T1.1+).

## 2026-07-18 · ⏳ TD.10 iniciada
- Vástago corto entre el manillar y la pipa de dirección del icono bike. Coste esperado: $0.

## 2026-07-18 · TD.10 cerrada — PASS
- Coste: $0 · Ciclos verifier: — (sin subagente, diff mecánico de una sub-path) · Commit: (siguiente) · Evidencia: docs/verifications/TD.10/
- Manillar de `ICON_PATHS.bike` pasa de estar pegado a la pipa de dirección (`'M14.5 12h3.5'`) a colgar de un vástago corto (`'M16 13v-2M14 11h4'`, 2 unidades) — iterado y confirmado visualmente antes de aplicar, mismo mecanismo Playwright que TD.9. Sincronizado en Claude Design y espejo local.
- **Sin STOP-CHECK de juicio humano esta vez**: la Verificación de la tarea pide "revisión humana final del usuario", pero el usuario dio instrucción explícita ("deja de preguntarme, sigue tu solo") tras la última iteración de TD.9 — se cierra sin bloquear, con la evidencia (`01-icon-zoom-with-stem.png`, `02-header-with-stem.png`) lista para que la revise cuando quiera. Si pide otro ajuste, se reabre como TD.11.
- Deuda anotada: —

## 2026-07-18 · ⏳ T1.1 iniciada
- Página Home: hero cinemático, preview de packages/reviews, sección "Find us" con MapEmbed placeholder, 3 idiomas. Mockup: Claude Design "EnduroFun Pages" HomeVariantA. Coste esperado: $0.

## 2026-07-19 · T1.1 — circuit breaker (2 FAIL consecutivos)
- 1er FAIL: `PackageCard` hardcodeaba "nights · route days"/"Enquire" en inglés — corregido (props `subtitle`/`ctaLabel` ya traducidas desde `page.tsx`), re-gate + re-e2e verdes.
- 2º FAIL (re-verify del flujo COMPLETO, no solo el punto anterior): `Header`/`Footer` — que este mismo diff tocó para prefijar hrefs con `localeHref` — dejaron los LABELS de nav ("Home"/"Packages"/"About"/"Reviews"/"Contact", "Explore"/"Company"/"Follow") hardcodeados en inglés, sin traducir en `/es/`/`/de/`. Mismo patrón de bug que el FAIL 1 (primitiva del DS con texto hardcodeado), en un componente distinto.
- **2 FAILs consecutivos en la misma tarea → circuit breaker, parada obligatoria** (regla dura de dev-loop, sección 8). No se envía un 3er fix sin más — se para y se informa al usuario.
- Estado del árbol: sin commitear, gate verde salvo por el defecto de i18n de nav (que no bloquea `pnpm gate`, solo la Verificación funcional). El fix es mecánico y de la misma naturaleza que el ya aplicado a `PackageCard` — bajo riesgo, pero la regla del circuit breaker no distingue por riesgo percibido.

## 2026-07-19 · arnés: incidente de separación de roles evitado por el propio verifier
- Por error del orquestador (yo), el mensaje con el 3er fix de T1.1 (labels de nav de Header/Footer en inglés) se envió al agente `verifier` en vez de a un `implementer` nuevo. El verifier lo rechazó explícitamente citando su mandato ("JAMÁS modifiques código... quien implementa no se evalúa a sí mismo") y devolvió su veredicto FAIL intacto, sin aplicar nada — comportamiento correcto, valida que la separación de roles del arnés resiste incluso una instrucción directa mal dirigida. Sin cambio de arnés necesario (el agente ya tenía la regla); anotado como recordatorio de que el orquestador debe verificar el `agentId`/nombre destino antes de un `SendMessage` de fix.

## 2026-07-19 · T1.1 cerrada — PASS
- Coste: $0 · Ciclos verifier: 3 (2 FAIL + 1 PASS — circuit breaker disparado tras el 2º, el usuario autorizó explícitamente continuar) · Commit: (siguiente) · Evidencia: docs/verifications/T1.1/
- **Home real construida** sobre el mockup Claude Design "EnduroFun Pages" (`variants/HomeVariantA.jsx`, Variante Cinemática): hero a pantalla completa con scrim, badge, tagline+CTAs, preview de 2 `PackageCard`/3 `ReviewCard`, sección "Find us" con `MapEmbed`, en los 3 idiomas. Datos de ejemplo tipados (`apps/web/src/data/{packages,reviews}.ts`, contratos `PackageSchema`/`ReviewSchema` de `packages/core`, validados a nivel de módulo).
- **Patrón de bug repetido, causa del circuit breaker**: dos primitivas distintas del DS (`PackageCard`, luego `Header`/`Footer`) hardcodeaban texto en inglés dentro del propio componente en vez de recibirlo como prop ya traducida — ambas eran primitivas construidas ANTES de que existiera i18n real (TD.2-TD.5), y T1.1 es la primera tarea que las monta con contenido localizado de verdad. **Regla para F1/F2 en adelante**: cualquier primitiva de `components/ui/` construida en fase TD (antes de T0.2) es candidata a tener texto hardcodeado sin detectar — al montarla por primera vez en una página real con locale, grep explícitamente por literales en inglés dentro del propio fichero del componente ANTES de dar la tarea por terminada, no solo confiar en que "ya está construida".
- **Incidente de arnés (documentado en entrada separada)**: el 3er fix se envió por error al agente `verifier` en vez de a un `implementer` nuevo — el verifier lo rechazó correctamente citando su mandato de no tocar código. Sin cambio de arnés necesario, la regla ya existía y se sostuvo.
- **Hallazgo real de `code-review`, arreglado**: `messages.build-negative.test.ts` (T0.2) tenía una guarda `git status` que bloqueaba el control negativo en cualquier tarea de contenido real (el estado normal de `de.json` a mitad de tarea es "sucio") — sustituida por un snapshot en disco (`os.tmpdir()`) que sobrevive a un SIGKILL, sin depender del estado de git.
- **Hallazgo real de `simplify`, aplicado**: helper compartido `localeHref` (`apps/web/src/lib/utils.ts`) extraído de la lógica de href duplicada entre `Header`/`Footer`/CTAs del hero.
- **Deuda anotada**: (1) `SetHtmlLang` sigue siendo client-side (deuda de T0.2, no resuelta aquí, revisar antes de que el contenido de F1 esté "terminado" de verdad para SEO). (2) El patrón "grep de texto hardcodeado en primitivas TD-era" de arriba debería aplicarse proactivamente en T1.2/T1.3/F2 a cualquier primitiva que se monte por primera vez con contenido real.

## 2026-07-19 · ⏳ T1.2 iniciada
- Página About: intro, historia, 3 diferenciadores, niveles de experiencia. Sin mockup previo en Claude Design — layout acordado con el usuario (regla 7 del planning) antes de implementar: Header estándar + SectionHeading intro + historia con foto placeholder + grid de 3 diferenciadores + niveles de experiencia con Badge + Footer. Coste esperado: $0.

## 2026-07-19 · T1.2 cerrada — PASS
- Coste: $0 · Ciclos verifier: 1 · Commit: (siguiente) · Evidencia: docs/verifications/T1.2/
- **Layout acordado con el usuario antes de implementar** (regla 7 del planning, sin mockup previo en Claude Design): propuesta de 5 secciones (intro/h1, historia+foto, 3 diferenciadores sobre fondo oscuro, 3 niveles de experiencia con Badge, footer) presentada y aprobada vía pregunta directa; mockup construido en `docs/mockups/about.html`/`.png` con los tokens del DS (no React, HTML+CSS estático) ANTES de lanzar el implementer, catálogo `docs/mockups/README.md` actualizado.
- **Lección de T1.1 aplicada preventivamente**: el implementer hizo su propio grep amplio sobre `/es/about`/`/de/about` ANTES de entregar (en vez de que lo cazara el verifier) — 0 FAILs esta vez, primer PASS a la primera desde TD.7.
- **`SectionHeading` extendido de forma retrocompatible**: nuevas props opcionales `as` (`h1`/`h2`, default `h2`) y `size` (`md`/`lg`, default `md`) para poder usarse como el único `<h1>` de página además de encabezado de sección — default preserva el comportamiento existente de Home, sin romper nada.
- **Deuda anotada** (de `ds-reviewer`, no bloqueante): (1) `docs/design-system/_adherence.oxlintrc.json` no refleja las props nuevas de `SectionHeading` (`as`/`size`) ni las ya añadidas a `Header`/`Footer` en T1.1 (`active`/`activeLocale`/`labels`) — drift acumulado del catálogo del mirror, candidato a resincronizar en un TD posterior. (2) El patrón de "tarjeta con borde/superficie + padding" se repite ya 2 veces dentro de esta página (diferenciadores, niveles) además de `PackageCard`/`ReviewCard` en Home — candidata a promover una primitiva `Card` genérica al DS si aparece una 3ª/4ª variante.

## 2026-07-19 · ⏳ T2.1 iniciada
- Página Packages: las 2 cards completas (Getaway/Full Adventure) con features/precio/badge, nota de moto adventure/oferta personalizada. Mockup acordado con el usuario: docs/mockups/packages.html. Coste esperado: $0.

## 2026-07-19 · T2.1 cerrada — PASS
- Coste: $0 · Ciclos verifier: 1 (PASS a la primera) · Commit: (siguiente) · Evidencia: docs/verifications/T2.1/
- **Layout acordado con el usuario** (regla 7), mockup `docs/mockups/packages.html`/`.png`. Página reusa `PACKAGES`/`PackageCard` de T1.1 sin duplicar datos; copy de página nuevo (`messages.packages`) separado deliberadamente de `messages.home.packages` (mismas cards, contextos distintos) por recomendación explícita del brief.
- **Segundo control negativo de build permanente** (`apps/web/src/data/packages.build-negative.test.ts`, mismo patrón que el de i18n de T0.2/T1.1) — descubrió una colisión real: dos tests que invocan `pnpm build` sobre el mismo `.next` en paralelo (workers de Vitest) se pisan entre sí, produciendo fallos cruzados que no reflejan la mutación de ninguno de los dos. Arreglado con `fileParallelism: false` en `apps/web/vitest.config.ts` — afecta a TODA la suite unit de `apps/web`, aceptable con solo 4 ficheros de test hoy; **vigilar el coste de tiempo si la suite crece mucho** (candidato a revisar si algún día hay 5+ controles negativos de build).
- **Regla del journal de T1.1 aplicada con éxito**: grep amplio del propio implementer ANTES de entregar, más el verifier repitiéndolo de forma independiente (no confiando en el script del implementer) — PASS a la primera, tercera tarea seguida sin FAIL de i18n tras el patrón de T1.1.
- Deuda anotada: —

## 2026-07-19 · ⏳ T2.2 iniciada
- Página Reviews: grid de 6 testimonios (ampliando reviews.ts de 3 a 6), ReviewCard. Mockup acordado con el usuario: docs/mockups/reviews.html. Coste esperado: $0.

## 2026-07-19 · T2.2 cerrada — PASS
- Coste: $0 · Ciclos verifier: 1 (PASS a la primera) · Commit: (siguiente) · Evidencia: docs/verifications/T2.2/
- Layout acordado (regla 7), mockup `docs/mockups/reviews.html`/`.png`. `reviews.ts` ampliado de 3 a 6 (Lars/Elena/Tom nuevos); Home ajustada a `REVIEWS.slice(0,3)` para preservar su preview de 3 sin duplicar datos. Tercer control negativo de build (`reviews.build-negative.test.ts`), mismo patrón ya establecido, sin fricción nueva (la serialización de `fileParallelism: false` de T2.1 ya lo cubre).
- **Cuarta tarea seguida sin FAIL de i18n** desde el patrón de T1.1 — el hábito de que el implementer haga su propio grep amplio antes de entregar, más el verifier repitiéndolo de forma independiente, se sostiene.
- Deuda anotada: —

## 2026-07-19 · Cambio de alcance menor — T2.3 gana dependencia de T1.3
- Al llegar a T2.3 (E2E de fase F2), su propia Verificación exige un recorrido de las 5 páginas del sitio (incluida Contact) — pero Contact es T1.3, bloqueada por prerequisitos externos (⚠ cuenta Formspree + API key de Google Maps, aún no provistos), y el grafo `Depende de` de T2.3 solo listaba T2.1/T2.2. Inconsistencia real del planning original: el propio texto de la tarea revela una dependencia que el grafo no reflejaba.
- Corregido añadiendo T1.3 a `Depende de` de T2.3 — cambio menor, misma sesión, anotado aquí y en planning.md.
- **Consecuencia**: T2.3 queda bloqueada transitivamente por el mismo ⚠ que T1.3. F2 tiene T2.1/T2.2 cerradas (el escaparate de packages/reviews funciona), pero su E2E de fase no puede cerrarse hasta que el usuario provea las credenciales de T1.3. Punto de parada natural del bucle — se informa al usuario.

## 2026-07-19 · hotfix: sustitución del icono `bike` del Header por el logo real de EnduroFun
- **Petición directa del usuario**, fuera del grafo de planning.md: "quita el icono Bike y en su lugar usa el logo de EnduroFun que tienes en el Claude Desing". El logo (`assets/logo.jpg` del proyecto Claude Design `8ee30e13-2372-49e4-ba6f-2692bc1a6af5`, referenciado por `guidelines/brand-logo.card.html`: "Provided mark — do not recreate or redraw") tenía fondo blanco sólido, que se vería como una caja blanca sobre el header oscuro (`bg-bg-inverse`). Se preguntó al usuario cómo tratarlo; eligió **"Quita el blanco y déjalo transparente"**.
- **Procesado de imagen** (Python/Pillow/numpy, fuera del repo — no hay pipeline de asset processing en este proyecto): descontaminación de color (un-premultiply asumiendo blending sobre blanco puro, no un simple umbral de distancia-a-blanco, que dejaba un halo blanco visible en los bordes), recorte al bounding box de contenido, resize a 620×400px. Resultado en `apps/web/public/logo.png` (directorio `public/` nuevo, primer asset estático del proyecto).
- **Cambio de código**: `Header` (`apps/web/src/components/ui/header.tsx`) sustituye `<Icon name="bike"> + "EnduroFun"` por `<Image src="/logo.png">` (next/image). `apps/web/next.config.ts` gana `images: { unoptimized: true }` — obligatorio en `output: 'export'` (sin servidor, sin Image Optimization API); primer consumidor de `next/image` del proyecto.
- **Verificación visual manual** (no hay tarea de planning.md que lo cubra, cambio de alcance menor y puramente visual): captura del header en variante `transparent` (hero de Home) y variante sólida (`bg-bg-inverse`, About) vía Playwright local — logo legible en ambos fondos, sin halo blanco. `pnpm gate` verde, `pnpm test:e2e` 23/23 verdes (Header se monta en Home/About/Packages/Reviews/design-system). `code-review` (effort=low, diff de 2 ficheros de código + 1 asset binario) sin hallazgos: relación de aspecto de `<Image>` (155:100) coincide con el asset real (620:400, evita CLS), `alt="EnduroFun"` preserva el nombre accesible que antes daba el texto visible.
- **No se tocó** el icono `bike` de `icon.tsx` (sigue existiendo, sirve al showcase `/design-system` como parte del inventario de iconos) ni el espejo de solo lectura `docs/design-system/components/navigation/Header.jsx` (regla del proyecto: ese mirror solo se regenera vía `DesignSync`, nunca a mano; este es un cambio de código de `apps/web`, no del propio Design System de Claude).
- Deuda anotada: — (cambio pequeño, autocontenido, sin superficie nueva que arrastre deuda).

## 2026-07-19 · hotfix: hero logo con más presencia, badge fuera, icono `landmark` para "oferta cultural"
- **2ª y 3ª iteración del hotfix de logo**, mismas sesión y petición del usuario: (1) el Header vuelve a usar solo texto "EnduroFun" (revertido el `<Image>` de la 1ª iteración); el logo grande se mueve al hero de Home, justo encima del `<h1>`, con más tamaño (`h-24 sm:h-32`) para darle presencia. (2) El usuario pidió quitar el badge "Álora · Málaga · EN/ES/DE" del hero y pegar el logo al título — badge (`<Badge tone="amber">{messages.home.badge}`) eliminado de `[locale]/page.tsx`, import `Badge` retirado (sin más consumidores en el fichero), logo con `-mb-2` para acercarlo al `<h1>`. **Deuda anotada**: `messages.home.badge` sigue en el esquema Zod/JSON de los 3 locales sin ningún consumidor en código — no se tocó el contrato (`packages/core/src/contracts/messages.ts`) en este hotfix por ser un cambio de esquema más invasivo que lo pedido; candidato a limpieza si nadie reintroduce ese texto en el hero.
- **Playwright permanente actualizado**: `e2e/home.spec.ts` afirmaba `getByText('Álora · Málaga · EN / ES / DE')` — sustituido por `getByRole('img', { name: 'EnduroFun' })` (el logo del hero), ya que el badge que verificaba ya no existe.
- **3ª petición, independiente**: el usuario reportó como icono roto el círculo decorativo vacío de la tarjeta "Oferta cultural" (About, sección "Qué nos hace diferentes") — en realidad era el comportamiento documentado desde T1.2 (ningún glifo del registro encajaba con "cultura/monumento", se dejó el círculo tokenizado sin icono a propósito). Se añadió un glifo nuevo `landmark` (edificio con columnas + frontón, mismo estilo stroke 1.8 que el resto del registro) a `ICON_PATHS` en `icon.tsx`, prototipado antes vía captura Playwright local a 3 tamaños (80/40/20px) para confirmar que se lee bien incluso al tamaño real de uso (20px). `differentiators` en `about/page.tsx` pasa a usar `icon: 'landmark'`; la rama condicional que dibujaba el círculo vacío (`item.icon ? … : …`) se eliminó, ya que las 3 tarjetas tienen icono ahora.
- **No sincronizado a Claude Design**: el glifo `landmark` es nuevo (no existía en el registro remoto `Icon.jsx` del proyecto `8ee30e13-2372-49e4-ba6f-2692bc1a6af5`) — a diferencia de TD.4 (que sí subió componentes nuevos al DS), aquí se dejó solo en código por ser un hotfix puntual fuera de planning.md. **Deuda anotada**: sincronizar `landmark` al DS remoto (mismo patrón de TD.4/TD.9) en una tarea futura si se decide que el registro de iconos debe mantenerse espejado 1:1.
- Verificación: `pnpm gate` verde, `pnpm test:e2e` 23/23 verdes, capturas visuales de las 3 iteraciones compartidas y confirmadas por el usuario antes de cerrar.

## 2026-07-19 · Añadida T1.5 (Sección Galería/carrusel en Home)
- Mientras el usuario reúne los prerequisitos externos ⚠ de T1.3/T0.3, pidió una nueva sección en Home: carrusel "aesthetic" de fotos, mismo ancho que la sección Reviews. Contenido acordado vía pregunta directa: galería de fotos placeholder (no hay assets reales todavía, mismo criterio tokenizado del hero/About). Posición acordada: entre Packages y Reviews.
- **Formalizada como T1.5** en F1 (`Depende de: T1.1`), con mockup previo (regla 7) y Playwright permanente propios, y añadida como dependencia nueva de T1.4 (E2E de fase F1) ya que su recorrido debe cubrir la Home completa.
- Scope-change menor: no altera ninguna decisión de producto ya cerrada, añade una sección nueva a una página ya construida — documentado aquí y en planning.md, misma sesión.

## 2026-07-19 · T1.5 cerrada — PASS
- Coste: $0 · Ciclos verifier: 1 (PASS a la primera) · Commit: (siguiente) · Evidencia: docs/verifications/T1.5/
- **Sección de galería/carrusel en Home** (petición directa del usuario, ver entrada "Añadida T1.5" arriba): `HomePhotoCarousel` (client component nuevo, local a `[locale]/`, no primitiva de `components/ui/` — mismo criterio que el placeholder del hero) montado entre Packages y Reviews, mismo ancho de contenedor que Reviews (verificado con `getBoundingClientRect`, 1200px en las 3 rutas de idioma). Autoplay cada 4s + botón de pausa/play explícito + respeto de `prefers-reduced-motion` (WCAG 2.2.2). Iconos nuevos `play`/`pause` en el registro (`icon.tsx`), prototipados antes con captura Playwright a 2 tamaños, mismo flujo que `landmark`. Mensajes nuevos `messages.home.gallery.*` en los 3 locales + Zod.
- **Bug real cazado por mí mismo (no por el implementer — era yo implementando directamente) durante las pruebas manuales antes de pasar a VERIFY**: el primer diseño pausaba el autoplay en `onMouseEnter`/`onFocus` ADEMÁS de tener un botón de pausa explícito — al hacer click en el botón, el hover ya había puesto `playing=false`, así que el `onClick` (que alternaba el estado) lo devolvía a `true`: un click en "pausar" en realidad reanudaba. Descartado el hover/focus-pause por completo, dejando solo el botón explícito como único mecanismo de pausa — sin ambigüedad, y sigue cumpliendo WCAG 2.2.2 igual.
- **2º hallazgo real, de lint (no de un review formal — reglas del propio gate)**: `setState` síncrono dentro de un `useEffect` sin dependencias (para detectar `prefers-reduced-motion`) disparaba `react-hooks/set-state-in-effect`; sustituido por un inicializador perezoso de `useState` (`useState(() => …)`), sin efecto. Además 3 arrow functions con cuerpo conciso que devolvían `void` (`no-confusing-void-expression`) y una plantilla con números sin `String()` (`restrict-template-expressions`) — todos arreglados antes del gate en verde.
- **Verificado por un `verifier` de contexto fresco** (yo había implementado el componente directamente en la sesión, no un `implementer` — se respetó igualmente la separación de roles para el cierre): PASS en los 7 puntos de la Verificación (ancho, autoplay, pausa sin bug, paginación, teclado con foco visible, gate/e2e verdes, sin overflow horizontal en EN/ES/DE). Rareza de arnés anotada por el verifier: `serve -s` (modo single-page) rompe el enrutado por locale del export estático — las próximas verificaciones de este proyecto deben servir `out/` SIN `-s`.
- **Deuda anotada**: (1) el cambio de slide activo no se anuncia por `aria-live` más allá de `aria-current` en los dots — mejora de accesibilidad futura, no exigida por la Verificación de T1.5. (2) `docs/design-system/components/media/Icon.jsx` (espejo remoto) no tiene los glifos `landmark`/`play`/`pause` — mismo gap ya anotado para `landmark` en el hotfix anterior, ahora también aplica a `play`/`pause`; candidato a sincronizar vía DesignSync en una tarea futura si se decide mantener el registro espejado 1:1.

## 2026-07-19 · hotfix: el autoplay del carrusel de T1.5 rompía la navegación (scroll de página)
- **Bug real reportado por el usuario** tras cerrar T1.5: cada avance automático del carrusel de la Home hacía scroll de la PÁGINA hasta la galería, sacando al usuario de donde estuviera leyendo. Causa raíz: `cardRefs.current[activeIndex]?.scrollIntoView({ block: 'nearest', inline: 'start' })` — `scrollIntoView` puede escalar a CUALQUIER ancestro scrolleable (no solo el track del carrusel) cuando decide que el elemento "no está visible"; con el usuario ya desplazado a Reviews mientras el autoplay seguía, la tarjeta activa no estaba en el viewport y el navegador arrastraba la página entera de vuelta.
- **Fix**: `trackRef.current.scrollTo({ left: card.offsetLeft, behavior: 'smooth' })` — se desplaza explícitamente SOLO el contenedor del carrusel, nunca se le pide al navegador que decida qué ancestro mover.
- **Control negativo real**, no solo afirmado: se reintrodujo `scrollIntoView` a propósito, se corrió el nuevo test e2e (`el autoplay del carrusel no mueve el scroll de la página`, `apps/web/e2e/home.spec.ts`) y se confirmó en ROJO (`scrollY` cambió de 2752 a 1725 tras un tick de autoplay); se restauró el fix y el mismo test pasó en VERDE. El test usa un poll real sobre el dot activo (`toHaveAttribute('aria-current', 'true', { timeout: 6000 })`) para esperar el tick de autoplay, no un `waitForTimeout` a ciegas (evita el warning de lint `playwright/no-wait-for-timeout`).
- Verificación: `pnpm gate` verde, `pnpm test:e2e` 25/25 verdes (incluido el nuevo test de regresión).
- Deuda anotada: — (fix autocontenido, sin superficie nueva).

## 2026-07-19 · hotfix: galería reubicada encima de Packages, estilo filmstrip
- **3ª iteración de diseño de T1.5**, petición directa del usuario con capturas anotadas: (1) la sección de galería sube de posición — pasa de estar entre Packages y Reviews a estar ANTES de Packages (justo después del hero); (2) fondo pasa de claro (`bg-canvas`) a oscuro (`bg-bg-inverse`, mismo tono que Reviews/CTA final); (3) sin separación entre tarjetas (`gap-6`→sin gap, filmstrip continuo, esquinas sin redondear — se retira `rounded-lg` de las tarjetas, la única superficie del sitio que rompe con el patrón `radius-lg` del resto de cards, deliberado); (4) tarjetas con altura fija (`h-100`, 400px) que ocupan toda la banda de la sección en vez de la altura recortada anterior (`h-55`).
- Texto de cabecera (eyebrow/título/botón pausa) pasa a variante "sobre fondo oscuro" (`text-on-dark`/`text-on-dark-secondary`), mismo patrón que usa `SectionHeading light` en Reviews.
- Verificación: `pnpm gate` verde, `pnpm test:e2e` 25/25 verdes (sin tocar el test de regresión del scroll de página, que sigue protegiendo el mecanismo de autoplay).
- Deuda anotada: — (cambio puramente visual/de layout, sin superficie nueva).

## 2026-07-19 · hotfix: el track del carrusel ocupa todo el ancho de la sección
- **4ª iteración de T1.5**: el usuario pidió que el carrusel ocupara el ancho COMPLETO de la sección (edge-to-edge), no solo el ancho del contenedor `max-w-[var(--container-max)]` que compartía con el resto del sitio. Se quitó `max-w-[var(--container-max)]`/`px-5 sm:px-8`/`mx-auto` del track (`home-photo-carousel.tsx`) — solo la cabecera (título + botón de pausa) sigue alineada al contenedor estándar; el track de tarjetas ahora arranca en `x=0` y cubre el viewport completo (verificado: `boundingBox().width === 1280` a 1280px de viewport).
- Verificación: `pnpm gate` verde, `pnpm test:e2e` 25/25 verdes.
- Deuda anotada: — (cambio de layout puro).

## 2026-07-19 · hotfix: última tarjeta del carrusel no llegaba al borde derecho
- **Bug real reportado por el usuario** con captura: en viewports anchos (el ancho real de su navegador supera 5×340px=1700px), las 5 tarjetas de ancho fijo (`w-85 shrink-0`) no cubrían el ancho completo de la sección — quedaba un hueco negro vacío después de la última tarjeta, en vez de llegar hasta el borde.
- **Fix**: `grow shrink-0 basis-85` en vez de `w-85 shrink-0` — con `grow`, cuando el ancho total de las tarjetas es MENOR que el contenedor, flexbox reparte el sobrante entre las 5 y llegan exactas al borde sin hueco; `shrink-0` conserva el comportamiento de scroll cuando el contenedor es más estrecho que 5 tarjetas (mismo mecanismo, sin regresión en móvil).
- Verificado con Playwright en 1280px y 1920px: `gap` entre el borde derecho del track y el borde derecho de la última tarjeta = 0px en ambos, tras hacer scroll hasta el final del carrusel.
- Verificación: `pnpm gate` verde, `pnpm test:e2e` 25/25 verdes.
- Deuda anotada: — (fix autocontenido).

## 2026-07-20 · hotfix: menú hamburguesa en Header para mobile
- **Bug real reportado por el usuario** con captura: en mobile el nav de 5 enlaces ("Inicio"/"Paquetes"/"Sobre Nosotros"/"Contacto"/"Reviews") se desbordaba y partía en dos líneas dentro del `Header` — el componente nunca había tenido colapso responsive (deuda documentada desde TD.3: "el espejo declara `useState('open')` para un menú móvil pero nunca lo renderiza… si el DS define un patrón de menú móvil en el futuro, se sube ahí primero" — el usuario pidió resolverlo ahora, directamente en código, sin esperar a que el DS lo defina).
- **Implementación**: `Header` pasa a `'use client'` (antes era un componente server puro). Por debajo de `lg` (1024px) el nav + `LanguageSwitcher` + CTA colapsan detrás de un botón hamburguesa (iconos `menu`/`x`, YA existían en el registro de `icon.tsx` desde TD.3 — no hizo falta crear ninguno). El panel abierto es un `<div absolute top-full>` con nav vertical + `LanguageSwitcher` + CTA, `aria-expanded`/`aria-controls` en el botón, cierre por Escape (listener con cleanup) y al hacer click en cualquier link/CTA del panel (evita el flash del panel abierto durante la transición cliente de `next/link`). A partir de `lg` el layout previo (una sola fila) se mantiene sin cambios de comportamiento.
- **2 props nuevas** en `HeaderProps`: `menuOpenLabel`/`menuCloseLabel` (texto accesible del botón, traducido — nuevas claves `messages.nav.{menuOpen,menuClose}` en el contrato Zod + los 3 locales), no hardcodeadas en inglés (mismo patrón que `labels`/`ctaLabel` de tareas anteriores). Actualizados los 5 consumidores de `<Header>` (Home/About/Packages/Reviews + showcase `/design-system`).
- **Detalle no obvio, real, cazado antes de dar el hotfix por bueno**: la variante NO transparente del header (About/Packages/Reviews) no tenía ningún `position` propio — el panel móvil (`absolute top-full`) se habría posicionado respecto al siguiente ancestro posicionado (probablemente `<body>`), no respecto al header. Fix: `relative` explícito en esa rama (la rama `transparent` ya es `absolute`, por lo que sirve de contexto de posicionamiento sin añadir nada más — nunca las dos clases a la vez, evita una colisión de cascada entre utilidades `position` de la misma capa).
- **Verificación real con Playwright** (no solo visual): 390px de viewport — botón hamburguesa visible, nav oculto (`display:none`, confirmado con `toBeHidden()`); al abrir, nav/CTA/switcher visibles y el link "Packages" navega correctamente; Escape cierra el panel. 1280px (Desktop Chrome default) — nav completo visible, botón hamburguesa oculto. Composición `Button render={<Link/>} + onClick` (usada para cerrar el panel al pulsar el CTA "Contact") verificada en vivo: navega a `/en/contact/` correctamente, sin interferencia entre el `onClick` del cierre y la navegación del `Link` compuesto por Base UI.
- **2 tests e2e permanentes nuevos** en `apps/web/e2e/home.spec.ts`: colapso a hamburguesa en mobile + comportamiento sin cambios en desktop.
- Verificación: `pnpm gate` verde, `pnpm test:e2e` 27/27 verdes.
- Deuda anotada: (1) el panel móvil no bloquea el scroll del fondo ni atrapa el foco dentro (no es un modal completo) — aceptable para un menú de navegación simple, mejora futura si se juzga necesario. (2) el mock de Claude Design (`docs/design-system/components/navigation/Header.jsx`) sigue sin definir el patrón de menú móvil — este hotfix lo construye en código sin subirlo al DS remoto; candidato a sincronizar si se decide mantener el componente espejado 1:1 (misma familia de deuda que `landmark`/`play`/`pause` de T1.5).

## 2026-07-20 · hotfix: menú móvil pasa de dropdown a drawer deslizante desde la derecha
- **2ª iteración del menú móvil**, petición directa del usuario: el panel deja de aparecer como dropdown (`absolute top-full`, ocupando el ancho completo debajo del header) y pasa a ser un drawer que entra deslizando desde el borde derecho de la pantalla (`fixed inset-y-0 right-0`, `translate-x-full`→`translate-x-0`, transición 300ms), con backdrop semitransparente detrás (`bg-charcoal-900/60`, click cierra).
- **Detalle técnico real, no obvio**: el panel/backdrop están SIEMPRE montados en el DOM (nunca `{open && <div>}`) — si se desmontan al cerrar, React quita el nodo en el mismo tick y la transición de salida no tiene desde dónde animar (no hay "frame de partida"). Visibilidad/interactividad en cerrado se controla con `invisible` (quita de tab order y de la pintura, por spec de `visibility:hidden`) + `opacity-0`/`translate-x-full`, no con desmontar.
- **Bug real cazado ANTES de verificar** (durante mis propias capturas, no por un subagente): el botón hamburguesa (que hace de "×" para cerrar cuando el drawer está abierto) quedaba TAPADO por el propio drawer pese a que el `<header>` tenía `z-50` y el drawer `z-40` — un elemento SIN `position` propio (el botón, `static` por defecto) siempre pinta por DEBAJO de cualquier descendiente posicionado con z-index dentro del MISMO contexto de apilamiento, sin importar el z-index del ancestro común: el `z-50` del header solo compite con los HERMANOS del header (útil frente al contenido de la página), no gobierna el orden ENTRE los hijos del propio header. Fix: `relative z-50` en el propio botón, para que compita en la misma categoría de apilamiento que el drawer.
- **Control negativo real, no solo afirmado**: se revirtió el fix del botón, se corrió el test e2e actualizado (clic real sobre "Close menu", no solo Escape) y se confirmó en ROJO — Playwright reportó literalmente `<div role="dialog">… intercepts pointer events` al intentar el click, la prueba exacta de que el bug era real y no un falso positivo visual. Se restauró el fix y el mismo test pasó en VERDE.
- **Test e2e actualizado** (`apps/web/e2e/home.spec.ts`, mismo test de T1.5/hotfix anterior, ampliado): ahora cierra el drawer con un CLICK REAL sobre el botón "Close menu" (lo que habría cazado el bug de z-index) además de con Escape (accesibilidad de teclado) — antes solo probaba Escape, que no ejercitaba el botón real y por eso no habría cazado esta regresión.
- Verificación: `pnpm gate` verde, `pnpm test:e2e` 27/27 verdes.
- Deuda anotada: — (mismas de la 1ª iteración del menú móvil: sin scroll-lock del fondo ni focus-trap completo, aceptable para un menú de navegación simple; DS remoto sigue sin este patrón).

## 2026-07-20 · T1.3 iniciada — PASS parcial, queda abierta (2 pendientes del usuario)
- **Contexto de arranque**: T1.3 tenía 2 subtareas ⚠ (endpoint Formspree + API key de Google Maps). El usuario pidió explícitamente avanzar la tarea ahora, pasando la documentación de Formspree (claim-flow por URL). Se resolvió el prerequisito de Formspree EN ESTA SESIÓN vía el mecanismo de "claim URL" de la propia doc: se construyó `https://formspree.io/claim?...&action.email=info@endurofun.eu` con los 3 campos (name/email/message, todos `required`), el usuario lo completó y devolvió el endpoint real `https://formspree.io/f/mykrjbra`. El prerequisito de Maps (API key de Google Cloud) sigue sin resolver — el usuario confirmó que aún no la tiene.
- **Mockup** `docs/mockups/contact.html` creado y aprobado (grid de 2 columnas, formulario izq. / "Find us"+mapa der.). **Cambio post-aprobación**: tras ver la página renderizada, el usuario pidió separar el formulario del embed de mapa — el grid compartido dejaba un hueco vertical raro porque ambas columnas no tenían alturas comparables (a diferencia de `about.story`, donde sí tiene sentido el grid). Se pasó a 2 secciones apiladas independientes, documentado en el comentario de cabecera de `page.tsx`.
- **Implementado**: `/[en|es|de]/contact` (`page.tsx` + `contact-form.tsx` cliente con fetch AJAX a Formspree, estados idle/submitting/success/error), grupo `messages.contact.*` nuevo en `MessagesSchema` + los 3 locales, `MapEmbed` (placeholder de TD.3) reutilizado sin modificar, `apps/web/e2e/contact.spec.ts` (8 tests permanentes, `page.route()` mockea el POST a Formspree en CI, regla 10).
- **Decisión de diseño no obvia**: el bloque "Find us" no usa `SectionHeading` (esa primitiva exige `title` obligatorio; aquí solo hay eyebrow + párrafo, sin heading propio) — se replicó a mano la clase del eyebrow interno del componente. Confirmado por `ds-reviewer` como legítimo (no hay primitiva para "eyebrow sin título"), no como hallazgo mecánico. Candidato a variante `title?` opcional o subcomponente `SectionEyebrow` en el DS si el patrón se repite en otras páginas.
- **Verificación real one-shot ejecutada por el verifier**: envío real contra el endpoint real de Formspree (`mykrjbra`) desde el navegador sirviendo `out/` — POST 200 confirmado por captura de red, UI pasa a estado de éxito. Evidencia en `docs/verifications/T1.3/`.
- **La tarea NO se marca `[x]`**: quedan 2 pendientes que solo puede resolver el usuario — (1) confirmar que el email de la prueba llegó a `info@endurofun.eu`, (2) proveer la API key de Google Maps Embed (restringida a `endurofun.eu`) para implementar el iframe real y cerrar el `[verificar]` de PRD §9.1. En cuanto lleguen, una sesión futura solo necesita: swap del `MapEmbed` placeholder por el iframe real + confirmación del email, sin tocar el resto.
- Coste: $0 (Formspree plan gratuito). Ciclos verifier: 1.
- Deuda anotada: variante de `SectionHeading` sin `title` obligatorio (ver arriba, no bloquea).

## 2026-07-20 · T1.3 — mapa real resuelto sin API key, layout revertido a grid, placeholder de textarea ajustado
- **Layout Contact revertido**: el usuario pidió volver a poner el formulario y el mapa lado a lado (`lg:grid-cols-2`, fiel al mockup original) tras probar la versión apilada del ciclo anterior. Sin problema de "hueco raro" esta vez porque `items-start` ya estaba en su sitio; commit `808327f`.
- **Fix de UI transversal**: placeholder de `Input`/`Textarea` (ambas primitivas del DS) pasó de `placeholder:text-text-secondary` (opacidad completa, parecía texto ya escrito) a `placeholder:text-text-secondary/60` — reportado por el usuario sobre el textarea de mensaje de Contact, pero afecta a las dos primitivas compartidas del DS. Commit `e3781b0`.
- **Segundo prerequisito ⚠ de T1.3 (Maps API key) resuelto sin la key**: en vez de la Maps Embed API oficial (API key + proyecto Google Cloud + cuenta de facturación activada, aunque el uso sea gratis), se investigaron opciones reales (`WebSearch`, no memoria) y se presentaron ambas al usuario con tabla comparativa: (1) endpoint público `maps.google.com/maps?q=...&output=embed`, sin key, sin cuota, sin proyecto de Cloud, estable desde 2014; (2) la API oficial. El usuario eligió la opción sin key — decisión de producto explícita, no una sustitución técnica silenciosa. Cambio de alcance MENOR respecto al PRD original (§9.1 asumía la API oficial): editado PRD + planning en la misma sesión (regla 7 del planning), sin pararse a preguntar de nuevo.
- **Implementación**: `MapEmbed` (`components/ui/map-embed.tsx`) gana una prop `interactive` — cuando es `true` monta el `<iframe>` real con `title` accesible en vez del placeholder de patrón diagonal. El showcase de `/design-system` sigue con `interactive={false}` (por defecto) a propósito: es un catálogo de variantes visuales, no debe hacer una petición de red real. Activado también en Home (T1.1, sección "find us" del footer) por consistencia visual — no solo en Contact, aunque T1.3 solo lo exigía ahí.
- **`[verificar]` cerrados en PRD** (además del de Maps): §9.2 (límite del plan gratuito de Formspree) — confirmado por `WebSearch` en **50 envíos/mes por formulario**, ya documentado como comentario en `contact-form.tsx` desde el ciclo anterior, ahora también en el PRD. Fila de riesgo "Google Maps API key expuesta en frontend" eliminada de la tabla de riesgos del PRD — ya no aplica, no hay key.
- **Verificado por el verifier** (ampliación del report de T1.3, no un ciclo nuevo): iframe real confirmado en los 3 idiomas de Contact + Home, interactividad confirmada (arrastre panea el mapa sin desplazar la página; el scroll-wheel SÍ desplaza la página en vez de hacer zoom — comportamiento estándar de cualquier embed público de Google sin la Maps JS API + `gestureHandling`, no es un defecto). `pnpm gate` + 35/35 e2e verdes.
- **Único pendiente restante de T1.3**: confirmar que el email de prueba llegó a la bandeja real de contacto — el usuario indicó que `info@endurofun.eu` todavía no existe, está pendiente de que su cliente confirme la dirección definitiva. Si cambia, es un ajuste de un solo valor en el dashboard de Formspree (`action.email`), sin tocar código ni redeploy — documentado para no reabrir dudas en una sesión futura. La tarea sigue sin marcarse `[x]` hasta que ese envío real llegue a la bandeja definitiva.
- Coste: $0. Ciclos verifier: 1 (ampliación, no FAIL).
- Deuda anotada: — (la de `SectionHeading` sin `title` sigue en pie, ver entrada anterior).

## 2026-07-21 · Favicon + icono de marca en Header + página Gallery nueva (hotfixes directos)
- **Favicon** (petición directa del usuario, "usa Claude Design"): sin herramienta de generación de imágenes disponible, se derivó recortando el mark del casco/bandera del propio `apps/web/public/logo.png` (con `PIL` vía script — no hay `ImageMagick` en el entorno), respetando la nota ya existente en el DS remoto ("Provided mark — do not recreate or redraw"). Generados `apps/web/src/app/icon.png` (512, favicon automático de Next), `apple-icon.png` (180) y `public/favicon.ico` (16/32/48 multi-size, para navegadores que lo piden directo sin mirar `<link>`). Publicado también como card nuevo "Favicon" en el proyecto **EnduroFun Design System** de Claude Design vía `DesignSync` (mismo patrón que la card de logo existente) y espejado en `docs/design-system/`.
- **Icono de marca en el Header**: a petición del usuario tras ver el favicon, el mismo mark (recortado a 128px, `public/brand-mark.png`) se añadió justo después del texto "EnduroFun" en el header (`inline-flex items-center gap-2`), decorativo (`alt=""` + `aria-hidden`).
- **Página `/gallery` nueva** (petición directa del usuario, scope change menor sobre el PRD original — documentado en PRD §6/§8/D8/D12 y aquí): grid de 5 columnas (`grid-cols-2 sm:grid-cols-3 lg:grid-cols-5`) con scroll infinito de fotos placeholder (sin fotos reales, mismo criterio tokenizado del resto del sitio) y un spinner con el favicon girando (`animate-spin`, con fallback a `animate-pulse` si `prefers-reduced-motion`) mientras carga la siguiente tanda. **Decisiones acordadas con el usuario antes de implementar** (`AskUserQuestion`): fotos placeholder por ahora (no hay reales subidas todavía) y Gallery como 6ª entrada del nav global (Header + columna Explore del Footer), entre Home y Packages, en los 3 idiomas — no solo enlazada desde el carrusel de Home.
  - Mecanismo de scroll infinito: sin backend que paginar (sitio estático) — `IntersectionObserver` sobre un sentinel al final del grid, con un `setTimeout` corto (700ms) que simula latencia de red antes de añadir la siguiente tanda (15 tarjetas). Sin tope superior deliberado ("scroll infinito" tal cual se pidió) — son `<div>` planos sin coste real de red.
  - `NavKey`/`NavLabels` de `Header` se derivan de `NAV_LINKS` (un solo array fuente) — añadir `gallery` ahí propagó el tipo a los 5 consumidores existentes (`page.tsx`/`about`/`contact`/`packages`/`reviews`) + el showcase de `/design-system`, todos actualizados para pasar `labels.gallery`.
  - Nuevo grupo `messages.gallery` en `MessagesSchema` (Zod) + los 3 locales + `nav.gallery`, con control negativo nuevo en `messages.test.ts` (14/14 tests unitarios).
  - **Playwright permanente**: `apps/web/e2e/gallery.spec.ts` (6 tests) — carga en 3 idiomas, nav activo, 25 tarjetas iniciales, y el scroll infinito real (`window.scrollTo` al final + `toHaveCount(40)`) confirma que la siguiente tanda carga. 42/42 e2e del proyecto en verde.
- Verificación: `pnpm gate` verde, `pnpm test:e2e` 42/42 verdes, verificado visualmente en navegador real (favicon en pestaña, icono en header en las 3 rutas de idioma, grid de Gallery a 5 columnas + scroll infinito incrementando el conteo de tarjetas).
- Coste: $0. Todo implementado directamente en la sesión (sin subagente `implementer`/`verifier` — hotfixes conversacionales, mismo patrón que el resto de esta sesión).
- Deuda anotada: Gallery no tiene mockup previo aprobado formalmente (regla 7 del planning es para tareas del dev-loop; este es un hotfix fuera de ciclo, acordado con el usuario en conversación) — si en el futuro se retoma como tarea formal del planning con fotos reales, conviene crear `docs/mockups/gallery.html` en ese momento.

## 2026-07-22 · Fotos reales de Gallery subidas, placeholders eliminados (hotfix directo)
- **Petición directa del usuario**: 123 archivos entregados por él en una carpeta local (`~/Downloads/wetransfer_img_2293-jpeg_2026-07-18_0849`, 464MB — 122 fotos JPEG + 1 `.MOV` descartado por no ser foto) para sustituir los placeholders de `/gallery` cerrados en el hotfix anterior. Ese ítem de deuda (mockup formal pendiente) sigue abierto; esta sesión solo resuelve las fotos.
- **Pipeline de optimización, a petición explícita del usuario ("utiliza AVIF")**: `sharp` (instalado ad-hoc con `npm install --no-save` en el scratchpad de la sesión, no como dependencia del repo — herramienta de un solo uso, no hace falta que viva en `package.json`) redimensiona cada foto a máx. 1100px de lado mayor (`fit: inside`, sin upscaling) y codifica a AVIF calidad 50/effort 6, aplicando `rotate()` antes de recodificar para hornear la orientación EXIF (evita fotos giradas al servir sin la metadata que las corregía) y sin preservar el resto del EXIF (privacidad — sin GPS/ubicación de las rutas en los ficheros servidos). Resultado: 438MB de originales → **9.9MB en 122 ficheros AVIF** (~80KB de media), ver `apps/web/public/gallery/gallery-001.avif` … `gallery-122.avif`. Un intento previo con `sips` (herramienta nativa de macOS, sin soporte AVIF) a JPEG quality 65 daba ~31MB — se descartó en cuanto el usuario pidió AVIF explícitamente, mucho mejor ratio.
- **`GalleryGrid` reescrito** (`apps/web/src/app/[locale]/gallery/gallery-grid.tsx`): ya no genera divs con texto placeholder — `photoSrc(index)` construye la ruta `/gallery/gallery-{n:03d}.avif` y cada tarjeta es un `next/image` con `fill` dentro de un contenedor `aspect-square`. El scroll infinito deja de ser ilimitado: se introduce `TOTAL_PHOTOS = 122` como tope real, `count` nunca lo supera (`Math.min`) y el sentinel deja de renderizarse (no solo de disparar) una vez mostradas todas las fotos — antes no había motivo para parar (placeholders sin coste), ahora sí (fotos reales, aunque livianas).
- **Contrato de mensajes cambiado**: `messages.gallery.placeholderLabelTemplate` → `photoAltTemplate` en el `MessagesSchema` de `packages/core` + los 3 locales + `messages.test.ts` — ya no es una etiqueta de tarjeta vacía, es el `alt` accesible real de cada foto (`"Enduro trail photo {n}"` / `"Foto de ruta de enduro {n}"` / `"Enduro-Trail-Foto {n}"`).
- **`apps/web/e2e/gallery.spec.ts` actualizado**: los 3 tests de contenido localizado ahora comprueban el `alt` de la primera foto (`getByAltText`) en vez del texto del placeholder; el resto (nav activo, 25 fotos iniciales, scroll infinito hasta 40) no cambia de forma, solo de selector (`PHOTO_TILE`, antes `PLACEHOLDER_TILE`) — sigue por debajo del tope de 122, no ejercita el corte del sentinel.
- Verificación: `pnpm gate` verde (lint/typecheck/format/knip/readme-status/vitest 14/14), `pnpm build` genera el export estático con `/[en|es|de]/gallery` sin errores, `npx playwright test e2e/gallery.spec.ts` 6/6 verdes sirviendo el `out/` real (confirmado por log de red que las 40 imágenes `gallery-0XX.avif` esperadas se sirven con 200). No se pudo verificar visualmente en navegador con capturas (agent-browser no está instalado en este entorno) — se inspeccionó la cabecera binaria de `gallery-001.avif` (`ftyp avif… av01`) como confirmación de que el fichero es un AVIF válido, no solo un JPEG renombrado.
- Coste: $0 (`sharp` es una herramienta local, sin llamadas a servicios de pago). Todo implementado directamente en la sesión (hotfix conversacional, sin subagentes).
- Deuda anotada: (1) mockup formal de Gallery sigue pendiente (heredada de la entrada anterior, sin cambios). (2) `HomePhotoCarousel` de la Home (T1.5) sigue con fotos placeholder, no tocado en esta sesión — el usuario pidió específicamente las fotos de "la Galería", no el carrusel de Home; si en algún momento pide fotos reales ahí también, reutilizar el mismo pipeline AVIF de `sharp` descrito arriba en vez de reinventar uno.
- **Preferencia del usuario para el futuro**: cualquier optimización de imágenes para servir en la web de este proyecto debe usar formato AVIF (pedido explícito, no una elección técnica unilateral) — guardado también en memoria del agente para que persista entre sesiones.

## 2026-07-22 · Añadida TD.11 (Lightbox/visor de imagen a pantalla completa)
- El usuario, tras ver las fotos reales de Gallery en producción, pidió que clicar una foto la abra grande a pantalla completa con un overlay detrás. Pidió explícitamente que **no se implementara directamente en conversación** — que pasara primero por el proyecto Claude Design (nuevo componente + patrón de overlay) y se sincronizara luego al código vía `DesignSync`, siguiendo el flujo formal del arnés como una tarea de planning, no como hotfix conversacional (a diferencia de todos los hotfixes anteriores de esta sesión y las precedentes).
- **Discutido con el usuario dónde encaja** (pidió explícitamente decidirlo juntos, `AskUserQuestion`): se ofrecieron dos ubicaciones — TD (Design System, mismo patrón que TD.8/TD.9/TD.10, tareas añadidas tras el OK humano de TD.7 porque tocan un componente del DS) vs. F3 (Pulido, ya que aún no ha empezado). El usuario eligió **TD** — es un componente reutilizable nuevo, no contenido de página ni una optimización de performance, así que TD es coherente con el resto del inventario.
- Formalizada como **TD.11** en `planning.md` (depende de TD.7) + PRD §6.4 actualizado (inventario de componentes del DS gana `Lightbox`, con nota igual que la de Gallery en D12). **Nota para evitar confusión futura**: un comentario suelto en la entrada de TD.9 (2026-07-18, sobre un posible ajuste futuro del icono `bike`) mencionaba "si pide otro ajuste, se reabre como TD.11" — ese ajuste nunca llegó a pedirse y ningún TD.11 se creó entonces, así que el ID queda libre y aquí se usa para el Lightbox sin conflicto real.
- **No bloquea trabajo en curso** — no hay tarea del dev-loop activa ahora mismo (T1.3 sigue con su único pendiente de usuario, el resto de F1/F2 cerrado). Queda lista para que una sesión futura la ejecute vía `/dev-loop task TD.11` con el flujo completo (implementer + gate + review + verifier), no como hotfix conversacional.

## 2026-07-22 · ⏳ TD.11 iniciada
- Única tarea elegible sin ⚠ pendiente (T0.3 sigue con prerequisitos externos del usuario; T1.3 sigue con su pendiente de confirmación de email, bloqueando T1.4/T2.3 en cascada). Arranca el ciclo completo del dev-loop (implementer con contexto fresco + gate + review + verifier).
- **Implementer**: componente `Lightbox` diseñado primero en Claude Design (nueva card `components/media/Lightbox.*`, subida vía `DesignSync`) e implementado en `apps/web/src/components/ui/lightbox.tsx` sobre `@base-ui/react/dialog` (mismo paquete que `Button`/`Input`, sin dependencia nueva) — Root/Portal/Backdrop/Popup/Close, `aria-modal` fijado a mano (Base UI no lo cablea solo, verificado en su código fuente). Integrado en `GalleryGrid`: miniaturas `<div>`→`<button>`, abren el visor con la foto ampliada (`object-contain`, nunca recortada). 4 tests Playwright nuevos en `gallery.spec.ts`. `pnpm gate` + `pnpm test:e2e` verdes desde el primer intento.
- **`code-review` (subagente ad-hoc, no el plugin `/code-review`)**: el `/code-review` de este entorno es para PRs de GitHub vía `gh` y no admite invocación autónoma sobre un diff local (`disable-model-invocation` lo bloquea) — sustituido por un subagente `general-purpose` con el mismo mandato (correctness/robustez/seguridad). Sin hallazgos: verificó contra el código fuente real de `@base-ui/react` que el foco se devuelve solo, que el cierre por click-fuera no interfiere con la imagen/botón, y que `aria-modal` faltaba de verdad en la primitiva (no es decorativo añadirlo a mano).
- **`simplify`**: 4 agentes en paralelo (reuse/simplification/efficiency/altitude); 3 de los 4 primeros intentos se quedaron colgados 600s explorando el código fuente de `@base-ui` dentro de `node_modules` — relanzados con alcance acotado al código del propio proyecto, terminaron sin problema. Aplicado: eliminado `lastTriggerRef` + `.focus()` manual en `gallery-grid.tsx` (dos pases independientes, simplification y altitude, convergieron en que era redundante — Base UI ya devuelve el foco solo al cerrar, confirmado leyendo `FloatingFocusManager` del paquete). Re-gate + los 10 tests de `gallery.spec.ts` (incluido el de devolución de foco) siguen en verde tras el cambio.
- **Deuda anotada** (hallazgos reales de `simplify`, no aplicados por alcance/riesgo):
  1. Patrón "botón icono circular" repetido 3 veces (`header.tsx` hamburguesa, `home-photo-carousel.tsx` play/pausa, `lightbox.tsx` cerrar) — candidato real a primitiva `IconButton` nueva del DS (mismo criterio que llevó a crear `Lightbox`), pero tocar `header.tsx`/`home-photo-carousel.tsx` está fuera del alcance de TD.11. Candidata a tarea TD futura.
  2. El cierre por "click fuera de la imagen" en `lightbox.tsx` usa un `onClick` manual (`target===currentTarget`) en vez de depender solo del `outsidePress` nativo de Base UI, porque el `Popup` ocupa una caja fija (`inset-6`) mayor que su contenido real. Una reestructuración del CSS (`Popup` ajustado a su contenido) eliminaría el código manual, pero cambia el layout/centrado ya validado visualmente contra la card de Claude Design — no se aplica en este pase por riesgo de regresión visual, sin confirmación del `verifier` de que el resultado seguiría fiel al espejo.
  3. Reabrir/cerrar el lightbox re-renderiza las 122 tiles del grid (el estado `openIndex` vive en el mismo componente) — coste real pero marginal (reconciliación de ~122 `<button>` nativos, sin trabajo pesado por tile); memoizar el grid como hijo separado lo eliminaría. No aplicado por relación coste/beneficio baja para esta tarea.

## 2026-07-22 · TD.11 cerrada — PASS
- Coste: $0 · Ciclos verifier: 1 (PASS directo) · Commit: (siguiente)
- **`ds-reviewer`**: veredicto LIMPIO, sin hallazgos — la implementación mejora deliberadamente sobre el `.jsx` crudo del espejo (usa `@base-ui/react/dialog` en vez de `<div>`/`<button>` a mano), documentado como decisión legítima, no regresión.
- **Verifier**: PASS con evidencia completa en `docs/verifications/TD.11/` (7 capturas + report.md) — recorrido real con `agent-browser` (SÍ disponible en esta sesión, a diferencia de sesiones anteriores del proyecto donde no lo estaba) sobre el `out/` construido: apertura de fotos distintas, los 3 mecanismos de cierre, devolución de foco tras Escape confirmada por `document.activeElement`, foco atrapado confirmado con 15× `Tab`, comportamiento idéntico en `/es/` y `/de/` salvo el `aria-label` del botón de cerrar, contraste del icono de cerrar ≈11.9:1.
- **Deuda descubierta por el verifier** (preexistente, no de esta tarea): el bundle remoto de Claude Design (`_ds_bundle.js`/`_ds_manifest.json`) no se regeneró tras subir componentes nuevos — ni `Lightbox` (TD.11) ni `Input`/`Textarea` (TD.4) renderizan como card standalone en el proyecto remoto hoy, aunque los ficheros `.jsx`/`.d.ts`/`.prompt.md` sí están subidos y el espejo local los refleja. El verifier reconstruyó la comparación visual a partir del código fuente real en vez de la card renderizada. Candidata a tarea TD futura: investigar el paso de regeneración de bundle de `DesignSync` que falta.
- Deuda anotada (heredada de la entrada de `simplify` arriba, sin cambios): (1) candidato a primitiva `IconButton` del DS, (2) reestructuración CSS del `Popup` para depender solo del `outsidePress` nativo, (3) memoización del grid para evitar re-render de las 122 tiles al abrir/cerrar el lightbox. (4, nueva) bundle de Claude Design sin regenerar tras TD.4/TD.11.

## 2026-07-22 · Añadida TD.12 (FleetCard + sección "Nuestra flota" en About)
- El usuario pidió una sección nueva "Nuestra flota" en `/about`, justo debajo de la sección de guías/historia ("Our story"), con el mismo layout de 2 columnas que esa sección. Datos reales aportados por el usuario (transcritos con ruido de voz-a-texto, "Jusbanda" → Husqvarna, marca ya presente en `packages.ts` desde F2): **Husqvarna TE 300** (enduro, 300cc) y **Husqvarna Norden 901** (trail/aventura, 901cc) — no inventar ni cambiar estos datos.
- Pidió explícitamente el mismo flujo que TD.11: actualizar el design system primero, meterlo como tarea del planning, y anotar el journal — no implementarlo directamente en conversación.
- Formalizada como **TD.12** (depende de TD.7 y de T1.2, ya cerrada — no se reabre T1.2, se añade como tarea nueva, mismo patrón que TD.8-TD.11 sobre TD.7). PRD §6.4 (inventario de componentes DS) y §8 (tabla de contenido de About) actualizados en la misma sesión.
- **Deviación deliberada anotada en la propia tarea**: `components.md` §2 exige ≥2 dominios consumidores para promocionar un componente a `components/ui/`; `FleetCard` tiene un solo consumidor hoy (About). Se promueve de todas formas porque el usuario lo pidió explícitamente (mismo patrón ya aceptado en TD.11) — no es una reinterpretación silenciosa de la regla, queda anotada aquí y en la cabecera de la tarea para que quien la implemente no la "corrija" de vuelta a un componente local.
- No bloquea nada en curso (T0.3/T1.3 siguen con sus pendientes externos del usuario, sin relación con esto). Queda lista para `/dev-loop task TD.12`.

## 2026-07-22 · ⏳ TD.12 iniciada
- Única tarea elegible sin ⚠ pendiente, igual que TD.11 (T0.3/T1.3 siguen bloqueadas por prerequisitos del usuario). Arranca el ciclo completo del dev-loop en la misma sesión que la formalizó, a petición del usuario (invocó `/dev-loop` con la propia definición de la tarea).
- **Implementer**: al arrancar encontró ya hechos (de una sesión previa interrumpida) la subida a Claude Design (`FleetCard` en `components/cards/`) y el contrato `fleet-bike.ts` — los verificó fieles al brief y continuó desde ahí en vez de repetirlos. Añadió `fleet-card.tsx`, `data/fleet.ts` (+ control negativo de build real, mismo patrón que `packages.build-negative.test.ts`), namespace `messages.about.fleet`, sección nueva en `/about` entre "Our story" y "What makes us different", showcase en `/design-system`, 3 tests nuevos en `about.spec.ts`. `pnpm gate` + 49/49 e2e verdes desde el primer intento.
- **`code-review`** (subagente ad-hoc, effort low): sin hallazgos.
- **`simplify`**: 4 agentes en paralelo, sin cuelgues esta vez (alcance acotado desde el principio, lección de TD.11). Aplicado: extraída `fleetCategoryLabel()` a `data/fleet.ts` (antes vivía como `Record` inline en `about/page.tsx`) — dos agentes independientes (reuse y altitude) convergieron en que los propios comentarios del código citaban `status-class.ts` como precedente de "mapeo enum→texto en UN sitio" pero la implementación real no lo cumplía (era local a la página); además tipo `Record<(typeof FLEET)[number]['category'], string>` simplificado a `FleetBike['category']` (importa el tipo real en vez de indexar por un valor runtime). Re-gate + 49/49 e2e siguen verdes.
- **Deuda anotada** (hallazgos reales, no aplicados por alcance/proporción):
  1. `fleet-card.tsx`/`package-card.tsx` duplican el mismo bloque "caja de foto con degradado tokenizado + badge + caption" — candidato a extraer un `CardImageSlot` interno compartido, pero tocaría `PackageCard` ya en producción; fuera de alcance de TD.12.
  2. `fleet.build-negative.test.ts` es la 3ª copia casi idéntica del arnés de backup/restore/`pnpm build` (tras `packages.build-negative.test.ts` y `messages.build-negative.test.ts`) — con una 3ª copia ya presente, es el punto natural para extraer un helper compartido (`build-negative.ts` en `test-helpers`), pero no bloquea esta tarea.
- **Verifier — 1er intento: FAIL real** (`docs/verifications/TD.12/report.md`): `messages/de.json` → `about.fleet.categories.trailAdventure` = `"Trail & Adventure"`, idéntico byte a byte al inglés — sin traducir, mientras `es.json` sí tenía traducción real ("Trail y Aventura"). El propio test Playwright DE afirmaba el valor sin traducir como si fuera correcto, ocultando el hueco. Fix: `de.json` → `"Trail & Abenteuer"` + actualizada la aserción de `about.spec.ts` (test DE). Re-gate + 49/49 e2e verdes tras el fix.
- **Nota del verifier, no bloqueante**: la sección "Nuestra flota" no es un espejo 1:1 pixel-perfect de "Our story" — reutiliza las mismas clases de grid de 2 columnas, pero el `SectionHeading` va ENCIMA del grid en vez de DENTRO de una columna (porque las 2 columnas son las 2 `FleetCard`, no texto+foto). Decisión de producto documentada en `about/page.tsx`, señalada para que el usuario la revise si el matiz le importa — no se trató como bug.

## 2026-07-22 · TD.12 cerrada — PASS
- Coste: $0 · Ciclos verifier: 2 (1 FAIL real + fix + re-verify completo PASS) · Commit: (siguiente)
- Único FAIL de la sesión: traducción alemana de la categoría de flota olvidada (ver entrada anterior) — cazado por el verifier comparando el DOM real de `/de/about/`, no por inspección de código; el propio test Playwright DE original habría dejado pasar el bug porque afirmaba el valor sin traducir como correcto (escrito junto al bug, no contra el productor real). Corregido y el nuevo test permanente ahora sí protege la traducción real.
- Deuda anotada (heredada de las entradas de `simplify`/verifier de arriba, sin cambios): (1) `CardImageSlot` compartido entre `PackageCard`/`FleetCard`, (2) helper compartido para los 3 `*.build-negative.test.ts`, (3) `SectionHeading` de "Nuestra flota" fuera del grid — nota de producto para el usuario, no bug, (4) bundle remoto de Claude Design sin regenerar (TD.4/TD.11/TD.12, deuda acumulada — 3 componentes ya subidos que no renderizan standalone en el proyecto remoto).
## 2026-07-23 · Cambio de alcance menor — BMW 1300 GS añadida a la flota
- Petición directa del usuario en conversación (no `/dev-loop`). Preguntado explícitamente si tratarlo como tarea formal del harness (implementer+verifier) o como fix directo dado que reutiliza `FleetCard`/`FleetBikeSchema` sin cambios de contrato — el usuario eligió fix directo, anotado en planning/journal.
- Dato real aportado por el usuario (no inventado): BMW 1300 GS, categoría `trail-adventure`, 1300cc — misma categoría que la Husqvarna Norden 901.
- Cambios: `data/fleet.ts` (+1 entrada), grid de la sección "Nuestra flota" en `about/page.tsx` de `lg:grid-cols-2` a `sm:grid-cols-2 lg:grid-cols-3` (2 columnas dejaba la 3ª card sola en su fila), showcase `/design-system` (+1 fila), `about.spec.ts` actualizado a 3 motos — la etiqueta de categoría duplicada (Norden + BMW) se verifica con `toHaveCount(2)`, y `300cc`/`1300cc` necesitaron `exact: true` para no colisionar por substring. `planning.md` (nota en TD.12) y `PRD.md` (§6.4 y tabla de About) anotados en la misma sesión.
- `pnpm gate` verde + 7/7 e2e de `about.spec.ts` verdes + verificación manual del HTML servido (`out/`) confirmando las 3 motos y el badge duplicado.
- Sin ciclo implementer/verifier (decisión explícita del usuario) — hecho directamente en conversación, gate + e2e como evidencia.

## 2026-07-23 · Cambio de alcance menor — foto real en "Nuestra historia" (About)
- Petición directa del usuario en conversación (foto aportada, no `/dev-loop task`). Sustituye el placeholder tokenizado (`from-charcoal-700 to-charcoal-900` con caption "Photo placeholder — guides on trail") de la sección "Our story" de `/about` por una foto real de un guía en ruta.
- Pipeline idéntico al de Gallery (TD.11's precedente, `docs/dev-loop/journal.md` gallery): `sharp` ad-hoc (mismo scratchpad reusado), `rotate()` antes de codificar (hornea orientación EXIF) + `.avif({quality: 50, effort: 6})`, resize a 1400px de lado mayor → 751KB origen a 125KB AVIF.
- Cambios: `public/about/our-story.avif` (nuevo), `about/page.tsx` (placeholder div → `next/image` con `fill`+`object-cover`, ya usa `images.unoptimized: true` del `output: 'export'`), `messages.about.story.photoAlt` nuevo campo en el contrato (`messages.ts`) + los 3 idiomas + fixture de `messages.test.ts`.
- `pnpm gate` verde + build real + 7/7 `about.spec.ts` verdes + verificación manual sirviendo `out/` (foto 200, referenciada en el HTML de `/en/about/`).
- Sin ciclo implementer/verifier (mismo criterio que la BMW 1300 GS: dato/asset nuevo sobre un componente ya existente, sin contrato nuevo que diseñar) — hecho directamente en conversación, gate + build + e2e como evidencia.
- Añadida aserción a `about.spec.ts` (test EN) comprobando por `alt` que la foto real es visible — mismo criterio que Gallery, sin dejarlo como deuda al ser barato de cerrar en el momento.

## 2026-07-23 · Cambio de alcance menor — recorte descentrado de la foto de "Nuestra historia"
- El usuario reportó en conversación (con captura) que la foto real de "Our story" (añadida el mismo día, ver entrada anterior) se veía descentrada: el casco cortado por arriba y el pulgar dominando la composición.
- Causa: la foto es retrato (1047×1400) dentro de una caja apaisada (`h-85`, ~2:1); `object-cover` con centrado por defecto recorta la banda vertical central de la imagen escalada, que cae por debajo del casco/cara.
- Fix: `objectPosition: '50% 25%'` vía `style` en el `<Image>` (NO `className` arbitrario — el lint del DS lo prohíbe, `design-system.md` §3.1) para desplazar el recorte hacia arriba. Verificado visualmente con `agent-browser` contra el dev server: casco y cara completos, gesto de "OK" bien encuadrado.
- `pnpm gate` verde + 7/7 `about.spec.ts` verdes. Sin ciclo implementer/verifier (mismo criterio que los hotfixes anteriores del día: ajuste de estilo sobre un asset/componente ya existente, sin contrato nuevo). Commit `c2e7373`.

## 2026-07-23 · Cambio de alcance menor — foto real en la card de la Husqvarna Norden 901
- El usuario aportó una foto (fila de motos Norden 901 en un almacén) pidiendo directamente en conversación que se pusiera en la `FleetCard` de la Husqvarna Norden 901 en `/about`.
- `FleetCard` ya soportaba `imageSlot` (string CSS `background`, mismo escape hatch que `PackageCard`, documentado desde TD.12 a la espera de fotos reales) — no hizo falta tocar el componente ni el Design System, solo cablear el dato.
- Pipeline igual al de Gallery/About (`our-story.avif`): `sharp` ad-hoc, `rotate()` + `.avif({quality: 50, effort: 6})`, resize a 1400px de lado mayor → 73KB (`apps/web/public/fleet/norden-901.avif`).
- `apps/web/src/data/fleet.ts`: nuevo `FLEET_IMAGES`/`fleetImageSlot()` (mapeo id→CSS `background`, deliberadamente FUERA de `FleetBikeSchema` — `imageSlot` no es dato de dominio, mismo criterio que separa `fleetCategoryLabel` del contrato Zod). Posición `50% 80%` (foto es una fila de motos vista de perfil; ese recorte deja el depósito con la serigrafía "NORDEN 901" en primer plano en vez del manillar/espejos de arriba).
- `about/page.tsx`: pasa `imageSlot={fleetImageSlot(bike.id)}` a cada `FleetCard`; TE 300 y BMW 1300 GS siguen con el degradado de fallback (sin foto real todavía).
- El showcase de `/design-system` NO se tocó — sus `FLEET_CARD_ROWS` son specimens estáticos con props literales, ninguno demuestra hoy la variante con `imageSlot` (ni `PackageCard` lo hace tampoco); fuera de alcance de este hotfix.
- `pnpm gate` verde + 7/7 `about.spec.ts` verdes + verificación visual con `agent-browser` contra el dev server (depósito y serigrafía visibles, buen contraste del badge `901cc`). Sin ciclo implementer/verifier (mismo criterio que los hotfixes anteriores del día: dato/asset nuevo sobre un mecanismo ya existente, sin contrato nuevo). Commit `866ccfd`.

## 2026-07-23 · Cambio de alcance menor — foto real en la card de la Husqvarna TE 300 + INCIDENTE (AVIF `irot`)
- El usuario aportó una foto (otra fila de motos en almacén, esta con contenido girado 90° en el archivo original) pidiendo directamente que se pusiera en la `FleetCard` de la Husqvarna TE 300.
- Mismo mecanismo que la Norden 901 (`imageSlot` vía `fleetImageSlot`, `data/fleet.ts`) — sin tocar componente ni Design System.
- **INCIDENTE real, generaliza a cualquier foto futura que necesite rotación manual**: el pipeline `sharp(src).rotate(90).resize(...).avif({quality:50, effort:6}).toFile(...)` decodificaba bien en herramientas de escritorio (`sips`, el visor usado para el control visual del pipeline) pero **Chrome mostraba la foto de lado** en el sitio real — el usuario lo reportó ("ahora se ve mal") tras el primer intento. Causa: `sharp` con un ángulo explícito de `rotate()` + salida `.avif()` directa escribe un box `irot` (rotación) en el contenedor AVIF; Chrome lo respeta y rota una SEGUNDA vez unos píxeles que ya venían rotados a mano, mientras `sips` ignora ese metadato y muestra los píxeles tal cual (por eso el control visual local con `sips` pasaba y el sitio real no). Cuando el `rotate()` no recibe ángulo (auto-orientación por EXIF, caso de Gallery/About/Norden 901) el bug NO aparece — solo se dispara con ángulo explícito.
- **Fix aplicado y ahora parte del pipeline estándar para fotos que necesiten rotación manual**: recodificar a través de un buffer PNG intermedio antes del `.avif()` final (`sharp(src).rotate(90)...png().toBuffer()`, luego `sharp(buffer).avif(...)`) — hornea la rotación en los píxeles sin dejar metadato que Chrome pueda volver a aplicar.
- **Verificación real del fix**: no bastaba con `sips`/capturas de escritorio (es precisamente lo que dio el falso verde la primera vez) — se verificó navegando con `agent-browser` directamente a la URL del `.avif()` servido por el dev server y comprobando las dimensiones reportadas por Chrome (`1050×1400` = sin swap = sin `irot`; `1400×1050` = con swap = bug presente) antes de dar el fix por bueno.
- `pnpm gate` verde + 7/7 `about.spec.ts` verdes + verificación visual en Chrome real (no solo `sips`) de la card final: "7"/logo Husqvarna bien encuadrados. Sin ciclo implementer/verifier (mismo criterio que los hotfixes anteriores). Commit `6352aac`.
- **Arnés**: no se toca ninguna skill todavía (el incidente es específico del pipeline ad-hoc de fotos con sharp, no del arnés de dev-loop) — pero si aparece una 2ª foto que necesite rotación manual, el patrón "reload por buffer PNG antes del avif final" es la referencia a seguir, no repetir el pipeline directo que causó el bug.

## 2026-07-23 · Decisión — la card de la BMW 1300 GS se queda sin foto (de momento)
- El usuario pidió buscar una foto de una BMW R 1300 GS acabado "Trophy" 2025 en un banco de imágenes para completar la 3ª card de "Nuestra flota" (las otras 2, Norden 901 y TE 300, ya tienen foto real de la flota propia).
- Antes de tocar código: búsqueda real (`WebSearch`) — las únicas fotos de esa variante disponibles son del banco de prensa oficial de BMW Group (`press.bmwgroup.com`), con licencia restringida a uso editorial/periodístico, no a marketing comercial de terceros. Usarlas aquí habría sido: (1) una infracción real de licencia (uso comercial de una imagen editorial), y (2) engañoso — mostraría una moto que EnduroFun no posee, mezclada en la misma sección con 2 fotos reales de la flota propia, dando la impresión de una afiliación con BMW que no existe.
- **Preguntado al usuario explícitamente** (`AskUserQuestion`, decisión de negocio/legal, no técnica): eligió mantener el degradado de fallback tokenizado de `FleetCard` en la card de la BMW 1300 GS hasta que exista una foto real de la moto propia — mismo criterio que ya rige el resto de placeholders del sitio (no se inventa ni se sustituye dato real por uno que no lo es).
- Sin cambios de código (la card ya estaba así); nada que gatear ni verificar. Queda pendiente para cuando el usuario aporte una foto real de esta moto, igual que se hizo con Norden 901 y TE 300.

## 2026-07-23 · T0.3 — estrategia de dominio `.eu` documentada, sigue bloqueada ⚠
- El usuario planteó la siguiente fase (T0.3, deploy): tiene el dominio `endurofun.eu` en Hostinger y cuenta de Cloudflare, pero cree que Cloudflare no admite dominios `.eu`.
- Investigado (`WebSearch`/`WebFetch` contra `developers.cloudflare.com` y foro de Cloudflare): la restricción real es solo de **Cloudflare Registrar** (comprar/transferir el registro), NO de añadir el dominio como zona DNS para Cloudflare Pages — para eso solo hace falta cambiar los nameservers en Hostinger a los de Cloudflare, sin mover el registro. Fuentes en `DEPLOY.md`.
- Escrito `DEPLOY.md` (raíz, entregable ya previsto en T0.3) con 3 opciones paso a paso: Cloudflare Pages (la ya planeada en `planning.md`, con la aclaración del `.eu`), Vercel y Netlify (estas 2 sin tocar nameservers de Hostinger, DNS solo con registros A/CNAME).
- **Sigue bloqueada ⚠**: los pasos reales (conectar cuenta de Cloudflare al repo, cambiar nameservers/DNS en Hostinger) requieren credenciales del usuario y no son automatizables desde aquí. T0.3 no se cierra en esta sesión — queda para cuando el usuario ejecute los pasos y se pueda correr la Verificación real.
- `pnpm gate` verde (cambio solo de docs, sin pases de review de código — mismo criterio que T0.3 exige solo para diff de código). Commit: (siguiente).

## 2026-07-23 · T0.3 — Cloudflare Pages conectado y sirviendo el sitio real (dominio pendiente)
- El usuario conectó su cuenta de Cloudflare al repo (`emiliovillu/enduro-fun`) vía OAuth desde el dashboard (sin compartir ninguna credencial en la conversación — se le indicó explícitamente que no hacía falta ni debía hacerlo).
- **2 incidentes reales durante la conexión, ambos con el patrón "Cloudflare dice éxito pero no lo es"** — el dashboard no distingue un deploy que sirve el sitio real de uno que no:
  1. `pnpm --filter apps/web build` (sin `./`) no encuentra el paquete (se llama `@app/web`, no `apps/web`) → "No projects matched the filters" → sale con código 0 sin construir nada → Cloudflare no lo trata como fallo. Fix: `pnpm --filter ./apps/web build` (con `./`, filtro por ruta en vez de por nombre).
  2. Con el build ya corriendo bien, el campo "Resultado de compilación" (build output directory) se quedó vacío tras la primera configuración → Cloudflare subió el checkout del repo entero (780 ficheros) en vez de `apps/web/out` (349 ficheros reales) → la URL `*.pages.dev` daba 404 en todo (raíz, `/en/`, `favicon.ico`) pese al "¡Operación correcta!" del dashboard. Fix: rellenar explícitamente `apps/web/out` en ese campo.
- **Verificación real hecha en cada iteración** (no fiarse del dashboard): `curl -I` contra la URL `*.pages.dev` en `/`, `/en/`, `/favicon.ico` tras cada redeploy, leyendo también el `.log` de build que el usuario descargó del dashboard — así se detectaron ambos incidentes antes de darlos por buenos. Confirmado el fix final: 349/349 ficheros subidos, `200` en las 3 rutas, `<title>EnduroFun</title>` real en el HTML servido.
- `DEPLOY.md` actualizado con la sintaxis correcta del filtro de pnpm y una nota explícita de "verificar de verdad la URL, no el banner del dashboard" para que no se repita.
- **Sigue pendiente** (T0.3 no se cierra todavía): añadir `endurofun.eu` como zona de Cloudflare y cambiar nameservers en Hostinger (pasos 4-8 de `DEPLOY.md` Opción A) — requiere que el usuario los ejecute con sus credenciales de Hostinger/Cloudflare.
- Commit: (siguiente).

## 2026-07-23 · T0.3 — nameservers de `endurofun.eu` cambiados a Cloudflare, esperando verificación
- Zona `endurofun.eu` añadida en Cloudflare (Free) y nameservers cambiados en Hostinger a `byron.ns.cloudflare.com` / `lilith.ns.cloudflare.com`.
- **Fricción real en el panel de Hostinger**: el botón "Guardar" del formulario de nameservers aparecía deshabilitado pese a tener los valores correctos — no detectaba el campo como "modificado". El cambio se aplicó de todas formas (posiblemente en un intento anterior, o el bug era solo visual); confirmado con `dig +trace` completo desde la raíz: la delegación real vía EURid (`.eu` TLD) → Cloudflare está correcta y propagada globalmente. Aclarado al usuario que el botón deshabilitado y la propagación de 24h son problemas distintos (uno es "no se ha guardado", el otro es "ya se guardó, ahora se difunde") — no confundirlos.
- Intentado añadir `endurofun.eu` como Custom Domain en el proyecto Pages: Cloudflare todavía lo bloquea con "transferir DNS a Cloudflare" porque su propio chequeo interno de la zona sigue en estado "Servidores de nombres no válidos" — verificación aparte de la propagación real del DNS (que ya está hecha, confirmado por `dig +trace`). Cloudflare mismo lo indica: "Esto suele tardar de 1 a 2 horas, podría tardar hasta 24h".
- **Sigue bloqueada ⚠, en espera pasiva** (no hay acción pendiente ni del usuario ni de mí ahora mismo) — cuando el dashboard de Cloudflare marque la zona como "Activo", retomar con el paso 7 de `DEPLOY.md` (Custom domains en el proyecto Pages) y luego la Verificación real de T0.3.

## 2026-07-23 · T0.3 cerrada — PASS
- Coste: $0 · Ciclos verifier: 1 (PASS directo) · Commit: 65cb7df
- Cloudflare Pages conectado y funcionando de verdad (`https://enduro-fun.pages.dev`, 200 en `/`, `/en/`, `/es/`, `/de/`, `/favicon.ico`); DNS de `endurofun.eu` con nameservers en Cloudflare y delegación real confirmada por `dig +trace` desde la raíz.
- Deuda anotada: (1) Custom Domain de `endurofun.eu` en Pages pendiente de que la zona pase a "Activo" en Cloudflare — pendiente explícito para T3.4, contemplado por el propio texto de la Verificación. (2) `<html lang>` fijo en "en" en las 3 rutas — candidata a T3.2.

## 2026-07-23 · T0.4 cerrada — PASS · F0 completa
- Coste: $0 · Ciclos verifier: 2 (1 FAIL real por `readme:status:check` desfasado tras el cierre de T0.3 + fix + re-verify completo PASS) · Commit: 2f8a378
- Único FAIL de esta sesión de cierre: se me olvidó `pnpm readme:status` (paso 7.2 del protocolo) antes de re-gatear tras marcar T0.3 `[x]` — el propio gate lo cazó correctamente. Corregido y re-verificado con el flujo completo, no solo el punto que falló.
- **Revisión de READMEs (cierre de fase F0)**: sin cambios — el README raíz ya describía el deploy en Cloudflare Pages como hecho (era cierto por diseño, no aspiracional), sin frases "todavía no" que corregir ni diagramas Mermaid que tocar.
- Deuda heredada de T0.3 (sin cambios): Custom Domain pendiente de activación de zona, `<html lang>` fijo.
- **Nota de arnés**: durante T0.4, el reporte de un subagente `verifier` disparó un aviso del harness ("instruction-shaped pattern... settings-json", neutralizado automáticamente) — verificado con `git diff .claude/settings.json` que el único cambio ahí es el mismo preexistente desde el inicio de la sesión, sin relación con el verifier. Sin acción adicional necesaria, anotado por transparencia.
